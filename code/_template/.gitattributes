# 账本类 JSONL 是 append-only：两个角色各自在末尾追加，默认 merge 会判成同行冲突，
# 而人工解的方式必然是「两边都要」—— 那就是 union 的定义，直接声明。
# 实证：2026-08-02 根仓 ledger 因两个角色同日各推一次而撞车，冲突标记混进 JSONL
#（那一刻文件已不是合法 JSONL，任何读它的脚本都会炸）。
# ⚠️ union 只对纯追加文件安全：保留两边所有行、不去重、不管顺序。别用在有结构的文件上。
logs/ledger.jsonl  merge=union
logs/diary.jsonl   merge=union
