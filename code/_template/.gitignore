# CI / 本地生成物（模板与 install-ci 共用）
**/node_modules/
**/__pycache__/
**/*.py[cod]
**/.pytest_cache/
**/.coverage
**/coverage/
**/dist/
**/build/
**/.venv/
**/venv/
**/.env
**/.env.*
!**/.env.example
!**/.env.*.example

# 运行时产物（脚本生成，不入仓）——
# 不忽略的话，只往 logs/ 写一行就会让 git status 变化，
# run_agent 的 landed 检查会被自己的 diary 写入骗成恒真。
logs/INDEX.md
logs/console.json
logs/diary.jsonl
logs/sessions/
logs/STOPLINE

# ⚠️ logs/ledger.jsonl **必须入仓**，故意不在上面那组里（2026-08-01 A-1 事故）。
# 它是逃生口问责账本：低频、正常运行一行不写，只在有人绕过闸门时才追加。
# 逃生口的全部合法性就是「绕了会被记下来」——账本不入仓 = 那句话是假的
# = 所有逃生口实际上是静默的。别改成 `logs/` 一刀切。checks/16 会机械兜底。
