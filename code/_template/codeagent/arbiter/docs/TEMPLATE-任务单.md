# 任务单 · <一句话标题>

> 复制本模板，填实后交给 `scripts/dispatch.sh <角色> <本文件路径>` 派活。
> 四个必填小节缺一不可——缺了执行者只能猜，猜错的成本比写清楚高得多。

## 1. 目标
<要达成什么。写结果，不写做法。>

## 2. 验收标准（必须可判定）
- [ ] <确定性命令 + 期望输出>
- [ ] <确定性命令 + 期望输出>

## 3. 允许触碰的目录
- 可写：<...>
- 只读：<...>
- 禁碰：<...>

## 4. 应代码化的验收项（交给 reviewer）
<本任务里可机械核验的点：diff 范围 / 行数 / 计数 / grep 事实 / 契约覆盖。
 reviewer 该为这些写脚本落 review/reviewcode/，而不是肉眼看。>

## 5. 交付前必过的自检门
<技术栈对应的确定性命令，例：ruff + mypy + pytest + 自跑 reviewcode。
 没过自检门不许交 reviewer。>

---
tier: simple | normal | hard
