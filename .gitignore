# 框架仓（starter）只跟踪可迁移的规范、文档、角色、脚本与模板 DNA。
# 在 code/ 下用 new_module.sh 创建的模块实例若为独立 Git 仓，默认不纳入 starter。
#
# ⚠️ 这是**白名单**（`/*` 先全忽略，再逐条放行）。它会**静默吞掉**任何未登记的
#    新增根级文件——不报错、不提示；`git add -A` 之后你以为加上了，其实没有。
#    框架 V1 丢失项目级骨架的物理机制之一就是这条。
#    **新增任何根级文件/目录，必须同时在下面加一行 `!/<名字>`，
#      并用 `git ls-files <路径>` 确认它真被跟踪。**
/*
!/.gitignore
!/.gitattributes
!/.github/
!/AGENTS.md
!/CONSTITUTION.md
!/README.md
!/CLAUDE.md
!/.env.example
!/agents/
!/code/
!/logs/
!/scripts/
!/control-panel/
!/blockers/
!/.claude/
!/contracts/
!/deploy/

# 生成物：由脚本产出，不入仓（入仓会让每次提交后工作树永远脏）
/logs/INDEX.md
/logs/console.json
/logs/diary.jsonl
/logs/sessions/
/logs/STOPLINE

# 逃生口问责账本：**必须入仓**，别加进上面那组（2026-08-01 A-1 事故）。
# 它和 diary 是两回事：diary 是高频事件流水（入仓会把「活落盘了」类检查骗成恒真），
# ledger 只在有人绕过闸门时才写一行。逃生口的全部合法性就是「绕了会被记下来」——
# 账本不入仓 = 那句话是假的 = 所有逃生口实际上是静默的。
# 此处不需要 ! 规则（/logs/ 已在上面白名单里，ledger 没被任何规则吞），
# 留这段注释是为了拦住下一个「顺手把 logs/* 都忽略掉」的人。checks/16 会机械兜底。

# 通用排除
**/node_modules/
**/.env
**/*.secret
**/secrets.env

# 数据护栏（开源前置，2026-08-08 裁决：代码可开源，真实时间安排数据不进仓）。
# Playwright 调试残留会把真实页面（含计时明细「01:24 英语学习…」）落成 yml/png/log，
# 一旦 tracked 就随开源泄露隐私。这类是临时产物，永不入仓。
**/.playwright-mcp/
# mongo dump / 导出快照：真实运行数据，只进独立的 cockpit-archived 备份仓，绝不进代码仓。
**/*.archive
**/cockpit-export-*.json
**/dump/
