# Python 字节码。
#
# 必须忽略，而且不只是为了干净：改名/脱敏之后，旧的 .pyc 里**仍然嵌着旧字符串常量**，
# 于是真实数据会以二进制形式被带进发布产物——而文本扫描器扫不出二进制里的东西。
# 2026-09-17 匿名化时真撞见过这一下。
__pycache__/
*.py[cod]

# 安装器产出。默认组装用手写的 deploy/docker-compose.yml，
# 这个目录是 `install.sh add` 换模块组合时才生成的，不入仓。
deploy/generated/

# 本地环境
.env

# 模块自己的本地变体（nginx-docker 的 env/.env.local 之类），同样带真值、不入仓。
# 样例一律叫 *.example，不受这条影响。
.env.local

# 模块目录里的 venv。nexus-core 与 ai-planner 的脚本约定解释器在
# code/backend/.venv/bin/python，所以它就长在工作树里，必须忽略。
.venv/
