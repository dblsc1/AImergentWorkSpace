# 文档中允许「暂不存在」的仓内路径 —— 有意的前向引用，必须逐条登记。
# 判据严格化后（人类 2026-07-28 裁决），未登记的死链一律红。
# 登记本身就是可审计的：谁加的、为什么，看 git blame。
#
# 报告协议点名、但按任务才产出的 canonical report：
codeagent/arbiter/docs/report.json
codeagent/programmer/docs/report.json
codeagent/programmer_reviewer/docs/report.json
codeagent/module_reviewer/docs/report.json
agents/cfo/docs/report.json
agents/consulter/docs/findings/report.json
# 模块内相对路径（在框架根不存在，生成模块后才有）：
code/backend
code/frontend
review/reviewcode
review/reviewreport
review/regression
module_docs/contract.md
module_docs/handoff.md
# 运行时生成物（.gitignore 忽略，跑起来才有）：
logs/INDEX.md
logs/console.json
logs/diary.jsonl
logs/sessions
agents/cfo/reviewer/session
logs/STOPLINE

# cfo_reviewer 详报里的跨仓引用：它审的是模块仓，报告落在框架仓。
# 这些路径在模块仓内真实存在，框架仓内不存在——不是死链，是跨仓引用。
# 根因见架构问题清单 A-12（历史叙事豁免名单未覆盖 reviewer 报告目录）。
code/backend/
code/backend/app/…
code/backend/requirements.txt

# 停线旗：按定义只在停线期间存在，平时不存在才是正常。
# 文档引用它是必须的（要告诉人怎么置旗），所以是正当前向引用。
code/nginx-docker
