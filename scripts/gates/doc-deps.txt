# 补充映射：自动推不出来的语义关联（文档讲了某功能，但正文里没写路径）。
# 格式：<路径 glob>  <文档>
# 主干是自动反查（文档里反引号写了哪个路径就依赖哪个），本表只补它推不出来的。
# 本表越短越好 —— 越长说明文档越没把它依赖的路径写清楚。
#
# 示例（按需启用）：
# code/*/code/backend/auth/*    code/*/module_docs/contract.md
