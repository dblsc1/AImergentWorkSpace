# 新模块 / 新项目开设指南

> 面向“快速开一个模块 / 网页项目 / 独立项目”的 agent。框架根指 clone 后的本仓顶层；本指南不假设它位于某个固定绝对路径。

## 一键脚手架

```bash
./ci/new_module.sh <相对 AIMERGENT_WORKSPACE_ROOT 的目标路径>
```

示例占位：

```bash
./ci/new_module.sh modules/example_service
AIMERGENT_WORKSPACE_ROOT=/path/to/workspace ./ci/new_module.sh experiments/example_app
```

脚本从框架根 `module_template/` 复制骨架，替换模块名和框架相对路径，再以 `main` 分支初始化目标 Git 仓。**建模块的本质 = 复制唯一模板 + 填真实事实 + 按需叠加治理。**

## 通用必读包

| 资料 | 框架内路径 | 作用 |
|---|---|---|
| 项目级铁律 | `AGENTS.md` | 工作模式、边界、协作流程 |
| 文档地图 | `docs/manual/文档地图.md` + `docs/manual/README.md` | 导航与文件夹速览 |
| 设计原因 | `docs/1 structure/README.md` | 权威架构说明 |
| 模块骨架 | `module_template/` | 新模块结构唯一事实源 |
| 角色基线 | `roles/{arbiter,backend,frontend,reviewagent}.md` + `orchestration.md` / `report-schema.md` | 模块角色卡薄引用 |
| 项目级角色 | `CFO_agent/{arbiter,consulter}/AGENTS.md` | 跨模块裁决与架构顾问 |
| 门禁工具 | `ci/` | 建模块、安装 CI、检查、合并 |

三条必须讲清的纪律：

1. **唯一事实源**：改骨架只改 `module_template/`，不改副本后再倒灌。
2. **级联方向**：框架/项目级 → 模块级 → 角色级；完整治理下下层只能加严。
3. **文档 DRY**：worklog / report.json / diary / handoff 各承载一个时间维度，各一写属主。

## 轻量模式

适用于突发想法、单人、无外部消费方：

- 保留：脚手架、Git、`.gitignore`、`.env.staging.example`、`code/` 和项目自检。
- 可暂缓：CFO、独立 reviewagent、worktree、canonical report、契约索引和远端合并门。
- 一个 agent 可兼任实现与自检，从模块根 `AGENTS.md` 与 `module_docs/rules.md` 接单，不加载单一受限角色卡。
- 一旦有其他模块/客户端调用，先把 `module_docs/contract.md` 填成真实对外契约。

一句话：轻量 = `new_module.sh` + 直接写 `code/` + 真实自检；骨架已保留原地升级能力。

## 完整治理模式

适用于多人、多模块、有外部消费方或需要独立审计的项目：

1. 在 `module_docs/rules.md` 选 `governed`，填技术栈、代码分区和确定性自检命令。
2. 填真实 `contract.md`：`provides`、`consumes`、路由、数据属主、env 边界和变更记录。
3. 根据实际需要激活 CFO + module arbiter + frontend + backend + reviewagent，角色卡薄引用框架 `roles/`。
4. 运行 `./ci/install-ci.sh <module-path>` 安装确定性 CI 与 hooks。
5. 首个切片走“开单 → 实现/自检 → 固定 candidate → reviewagent exact 独审 approved → merge gate”。

如模块将部署到网关，由项目填写真实 nginx 入口；`.env.staging.example` 只写变量名与无敏占位，真实值通过运行环境注入。

## 验收清单（7 条）

> 完整治理执行全部 7 条。轻量模式可暂缓多角色、canonical report、独立审核和远端合并，但 Git、`.gitignore`、密钥不入库和项目自检不可裁剪。

1. **结构齐**：`codeagent/` 四角色 + `module_docs/` 五件 + `code/` + `review/` 全在，空目录 clone 后不丢。
2. **占位清零**：已生成模块中 `{{MODULE_NAME}}` 和 `{{FRAMEWORK_ROOT}}` 零残留。
3. **级联可解析**：模块/角色 `AGENTS.md` 只引用当前框架内部的 `AGENTS.md`、`roles/`、`ci/`，无作者机器绝对路径。
4. **契约成文**：有外部消费方时，`contract.md` 是真实事实，无语义占位。
5. **门禁绿**：模块 `ci/gates/run-gates.sh` 与 `run-tests.sh` 通过；本任务有 canonical report 时通过 `check-report-schema.sh`。
6. **归属 + 落地**：完整治理的 commit 有唯一 `Agent-Attribution`。配置远端后才检查真实 push/remote tip；starter 本地冒烟不为测试伪造或新建远端。
7. **依赖合规**：只依赖已发布契约，无跨模块直连。

## 冷启动读取顺序

轻量：模块 `AGENTS.md` → `module_docs/rules.md` → `code/`。

完整治理：框架根 `AGENTS.md` → 模块 `AGENTS.md` → 框架 `roles/<role>.md` → 模块角色卡 → `module_docs/` → 所依赖契约。
