# 新模块 / 新项目开设指南

> 给"快速开一个新模块 / 新网页项目 / 新独立项目"的 agent。照本文知会资料 + 跑验收清单即可。
> 路径约定：母代码在 `/srv/aimergent/0`（简称 `0/`）；本模板在 `/srv/aimergent/sample-workspace`（ROOT 级，**不在 `0/` 里**）。

## 一键脚手架（先记这条）
```
0/ci/new_module.sh <相对 /srv/aimergent 的目标路径>
# 例：new_module.sh 0/functions/foo      （本项目教学功能模块）
#     new_module.sh 0/web_modules/bar    （本项目其余 web 模块）
#     new_module.sh dev/time-management   （新独立项目）
```
脚本做三件事：从 `sample-workspace/module_template`（唯一事实源）复制骨架 → 自动替换 `{{MODULE_NAME}}` → `git init`。
**开新模块/新项目的本质 = 跑这条 + 填 contract + 装门禁 + 走审核门。**

## A. 通用必读包（三种目标都要知会 agent）
| 资料 | 路径 | 作用 |
|---|---|---|
| 铁律 / 级联顶点 | `0/AGENTS.md` | 16 条铁律、协作流程、文档体系 |
| 文档地图 | `0/docs/manual/文档地图.md` + `README.md` | "想改什么动哪个文件" + 文件夹速览 |
| 设计为什么 | `0/docs/1 structure/README.md` | 权威架构说明 |
| 模块骨架 | `sample-workspace/module_template/` | 新模块结构唯一事实源 |
| 四角色基线 | `0/roles/{arbiter,backend,frontend,reviewagent}.md` + `orchestration.md`/`report-schema.md` | 模块角色卡**薄引用**这些 |
| 门禁工具 | `0/ci/`：`new_module.sh`/`install-ci.sh`/`run-gates.sh`/`merge-to-main.sh`/`hooks`/`new_task_id.sh` | 建 / 审 / 合 |

**三条必须讲清的纪律**
1. **唯一事实源** —— 改模板只改 `sample-workspace/module_template`，绝不改副本。
2. **级联方向** —— 项目级 → 模块级 → 角色级，下层**只能加严不能覆盖**上层。
3. **四件套 DRY** —— worklog / report.json / diary(jsonl，`ci/log_event.sh`) / handoff.md（arbiter 独占），各一个写属主。

## B. 分目标增量
### 目标 1 · 复刻文档框架格式（到新地方）
把 `0/` 当母代码样板，复制这六样"框架 DNA"：`AGENTS.md`（铁律）、`docs/manual`（导航）、`docs/1 structure`（设计）、`roles/`（角色）、`ci/`（门禁）、`sample-workspace/module_template`（骨架）。知会重点 = 上面三条纪律。

### 目标 2 · 本项目开新模块
`new_module.sh 0/functions/<name>`（教学功能）或 `0/web_modules/<name>`（其余 web）。建完 agent 只需：
- 填 `module_docs/contract.md`（对外唯一事实）+ `rules.md`；
- `code/backend|frontend` 写码；跑 `0/ci/install-ci.sh` 装门禁；
- **只准依赖平台层**（auth/llm/market_data/student_data）的契约；
- 定 nginx 路径前缀 `/<name>/`；CFO 登记 CONTRACTS-INDEX。

### 目标 3 · 新独立项目（如 time-management）
`new_module.sh dev/time-management`（独立仓，git 已 init）。角色齐五个：
**CFO（项目 arbiter，跨模块，复用 `0/CFO_agent/arbiter` 角色卡）+ module arbiter + frontend + backend + reviewagent**，各自 `codeagent/<role>/AGENTS.md` **薄引用** `0/roles`。
额外知会：对外 `contract.md`、依赖哪些平台契约、nginx 入口、`.env.staging.example` 密钥占位；冷启动读序 = `AGENTS.md` → 角色卡 → `module_docs` → 依赖契约。首个切片走"填码 → 测试 → reviewagent 独审 approved → 合"。

## B0. 轻量模式（突发奇想 / 单人 / 无外部消费方）
不想上项目级重架构时，只留核心、砍掉 ceremony：
- **保留**：`new_module.sh` 骨架、`git init`、`.gitignore`（密钥永不进 git）、`code/` 写码。
- **可砍**：CFO / 项目 arbiter、CONTRACTS-INDEX 登记（没人依赖你就不需要）、独立 reviewagent 走审核门、worktree 隔离、多角色并发。
- **一个 agent 兼多角色**：自己写、自己自检，不必派四个。
- **唯一别砍**：将来若别的模块要调你 → 补 `contract.md`；密钥走 env 占位、不进 git。
- **升级路径**：哪天要长大，再补角色卡 + 审核门 + 契约登记，**骨架不用重开**。
> 一句话：轻量 = `new_module.sh` + 直接写 `code/`。重量级那套（CFO/多角色/审核门/契约登记）是**按需叠加的 ceremony，不是开模块的前提**。

## C. 验收清单（7 条）
> 完整治理模式执行全部 7 条；轻量模式按 B0 裁剪：第 3/5/6/7 条中的多角色、canonical report、审核/合并门、远端与契约登记只在升级后强制；但 Git、`.gitignore`、密钥不入库与项目自检不可裁剪。
1. **结构齐**：`find <新模块>` 比对模板 —— `codeagent/` 四角色 + `module_docs/` 五件（contract/rules/reviewlog/report/handoff）+ `code/` + `review/` 全在。
2. **占位清零**：`grep -rn '{{MODULE_NAME}}' <新模块>` 无残留。
3. **级联无倒挂**：模块 / 角色 `AGENTS.md` 只加严，没覆盖 `0/AGENTS.md`；角色卡是薄引用不是重抄。
4. **契约成文**：有外部消费方时，`module_docs/contract.md` 是真实对外事实，不是模板占位；纯内部轻量项目可暂缓。
5. **门禁绿**：`0/ci/gates/run-gates.sh` 通过；`report.json` 过 `check-report-schema.sh`。
6. **归属 + 落地**：commit 带 `Agent-Attribution`；`git ls-remote` 核实真推送；**不自审自批**（reviewagent 独立审）。
7. **依赖合规**：只依赖平台层契约，无跨模块直连。

## 范例指引
- 文档纪律范本：`0/functions/stock_tycoon`（worklog/handoff/reviewlog 结构齐、reject→approve 闭环）。
- 搬家范本：`0/functions/copycat`（老仓重写 + 特征测试兜底）。
