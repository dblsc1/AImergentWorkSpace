# 2026-07-30 · consulter · 路径判据注释行豁免（CFO 实报，字面量类）

CFO 立障（未入仓便条）：四模块 run-gates 恒红于「硬编码绝对路径」，唯一命中是
dispatch.sh:58 一句**讲**坏路径之坑的注释——判例库「字面量类」教科书案例：
报告字面量缺陷的文字自己成为新实例。CFO 判不阻塞、建议判据排除注释行，均正确
（注释路径零可执行伤害；逼改文案=判据不许人讨论问题本身）。

修：run-gates 路径扫描命中行过滤 `#`/`//` 开头的注释行。沙箱红/绿：
前缀仅在注释→✅ 无未豁免禁止路径；真实代码行→命中并点名 file:line。

另记两笔：
- CFO 便条的「解除标志」写反了（`copycat 的`@dispatch.sh 现在就命中，一入仓 15 号
  立刻逼销）——好在走了修法①注释保留，便条无需入仓直接删即可。给 CFO 提醒：
  解除标志要写「修好之后才会出现的东西」，不是「现状就有的东西」。
- 本轮插曲：框架仓被临时挪走（用户已归位），未提交修复一度悬空——幸存。
  重申仓库职责纪律：小修也应尽快 commit，未提交状态是唯一无保险的状态。

会话重心已切换（用户裁定）：架构优化归 X_structure fork 会话；本会话以
time-management 推进为中心，框架只修阻塞/摩擦项。
