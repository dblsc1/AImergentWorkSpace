# 任务单：科普定位调研入仓 + v5 rebase 推送（consulter，2026-08-02）

## 目标

三路调研（world model vs LLM / 高低模型路由评测 / 新手提效证据）汇总为 reference 文档入仓，
含面向 vibecoding 新手的可视化教学功能清单（用户种子 S1/S2 + 扩展提案）；
将本地 v5 与远端（另一 checkout 领先 25 笔）rebase 合流后推上 origin。

## 可判定的验收标准

- `agents/reference/科普定位-新手第一个多agent架构.md` 在仓且被 Git 跟踪（`git ls-files` 可见）
- consulter worklog + report.json 同批更新，report git.base 是 HEAD 的真实祖先
- `git push origin v5` 成功，`git status` 干净，本地与远端 tip 一致
- 逃生口使用记录（logs/ledger.jsonl 增量）已入仓

## 可触碰目录

- `agents/consulter/`（自己留痕）
- `agents/reference/`（调研产物）
- `logs/`（账本，检查项自动放行）

## 自检门

- pre-commit 着陆检查全绿（14 项）
- arbiter-push.sh 推送门通过（逃生口使用需记账入仓）
