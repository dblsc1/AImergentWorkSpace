# 任务单 · 路签机制六条缺陷落地（F4 → 层② → F3 → F2 → F5/F6 → F7）

派活方：CFO（回复 2026-08-02 consulter 报告）· 执行者：consulter · tier：hard

## 目标

把上一轮 consulter 报出、CFO 已逐条裁决的六条缺陷落成代码，每条都留下**能重现拦住它的断言**。

| 条目 | 缺陷 | 裁决 |
|---|---|---|
| F4 | 路签拒发不记账，问题规模查无实据 | 修：补 `lease_denied` 事件 |
| 层② | 「完工即自动还签」无落点 | 落 `mission_start.sh` 拒发分支；只印强收判据+可粘贴命令，**不自动强收** |
| F3 | 角色自动推断对 J3 五条 canonical 路径全失效 | 修：推断覆盖五条路径；selftest 逐条喂 |
| F2 | 写区粒度棘轮（五天 4 片→8 片，只放宽不收窄） | 修：**只警告不拦**，警告须打印该角色签史 |
| F5/F6 | `review_target` 只断言祖先不断言覆盖；非审查轮次被迫回填 | 修：合并门加覆盖断言；schema 允许 consulter 显式声明非审查轮次 |
| F7 | 推送门全绿时吐 `comm: not in sorted order` | 修：两处 sort 加 `LC_ALL=C` |

## 可判定的验收标准

1. 六条各一个 commit，trailer `Agent-Attribution: consulter@root+<任务id>`。
2. 每条在 `scripts/selftest.sh` 留一条新断言（或强化既有断言）。
3. **每条断言做红/绿反向验证**：把修好的实现改坏 → 断言必须真变红；恢复 → 复绿。
   两次的真实退出码与关键输出行写进 worklog。
4. F3 的断言必须把**五条 J3 canonical 路径逐条**喂进沙箱，只喂一条判不合格。
5. `scripts/selftest.sh` 全量 FAIL=0。
6. `scripts/gates/check-report-schema.sh` 在本分支退 0，且**打印出被验对象**（不接受空区间绿）。

## 可触碰目录（写边界）

- 可写：`scripts/`、`agents/consulter/`
- 只读：其余全部。**`agents/cfo/` 明令不得动**（CFO 本轮指令）。
- 不 push（CFO 自己推）。

## 自检门

`scripts/mission_complete.sh`（pre-commit 主体）+ `scripts/selftest.sh` + `scripts/gates/run-gates.sh`。
