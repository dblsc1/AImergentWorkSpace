# 2026-07-29 · consulter · 审前任撞车 commit cbce175 + 判例库沉淀 + 用户目标口述入仓

## 一、审 cbce175（前任 consulter 在本 checkout 的撞车提交，已停手未推）

我不是作者，可审。**verdict: approved（带修补）**。

### 已机械核实

- mission_start#4 修复（认两处布局）：
  - 绿：`mission_start.sh consulter <任务单> <写区>` 整单通过、退 0、发签成功（审后还签）。
  - 红：ghostrole → 第 4 项 ❌，两处路径都点名。
  - 这正是我上午记进 findings 的「项目级角色在框架仓恒不过起飞单」——修对了类。
- 全景文档为纯新增 + 文档地图登记，无越界改动。

### 审出的缺陷（本 commit 修补）

1. **selftest 编号撞车**：其新断言标「35」，与我先落的 #35 重复（输出 35/36/35）。
   → 重编为 #37。并行会话各自数到 35——编号本身也是一种「计数」，同类风险。
2. **全景第 35 行写回旧分工表述**「它审 CFO 的活」——与 supervision.md 冲突，
   实证了矩阵断言担心的漂移。→ 改为指回矩阵，并把全景纳入 #35 覆盖（3/4→4/4，
   已红/绿重验：抹引用 → FAIL(3/4) 退 1；恢复 → 退 0）。
3. **工作区遗留未提交改动**（全景死表头清理）——作者停手时漏提交，随本 commit 收编。

## 二、判例库新增（前任点名 + 用户转达授权）

- 「恒定答案类」升级为硬要求：**新增断言必须同批红/绿反向验证，真实退出码入留痕**。
- 注脚：验证者自己的管道 `$?` 会撒谎（本日三次实证，第三次连 commit 被拒都没看出来）。

## 三、用户目标口述入仓

`agents/reference/用户目标口述-可复现工作机制.md`：总目标 + 四大手段原始记录，
标注中断缺口与现框架五处差异点（D1–D5，待人类裁决）。这是方向性输入，不是条文。

## 四、推送门实测撞出的两个发现 + 用户目标入卡（同日追加）

- **check-report-schema 分支模型不匹配**：要求 git.base 是 main merge-base 的祖先——
  假设短 feat 分支；v5 是长期分支（divergence 点 211bdec），任务级 base **恒红**。
  「恒定答案类」新实例。当下按门禁模型把 report base 改为 211bdec 解锁推送，
  任务粒度由 worklog 承载；门禁长期方案（认长期分支 or 全走短分支）待人类裁决。
- **生成物漂移**：`.claude/agents/consulter.md`（子代理 prompt，未跟踪）不随源再生，
  已停在旧版卡。本轮由最新 AGENTS.md + 审查提示词重新拼接生成；长期宜加
  新鲜度断言（13-gates-fresh 同形状）。
- **用户目标入卡**：`agents/consulter/AGENTS.md` 新增「目标」节，指向
  `agents/reference/用户目标口述-可复现工作机制.md`，并已进入重新生成的子代理 prompt。

## 遗留

- 三 commit（6c6d1ee/f3f9db9/62c2f91）+ 本 commit 仍待 CFO 交叉审（我不自批）。
- cbce175 属前任所写、我已审 approved——按矩阵此审有效（谁写的谁不审）。
- 推送等用户跑逃生口。
