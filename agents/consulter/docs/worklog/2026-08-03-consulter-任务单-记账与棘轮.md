# 任务单 · 逃生口记账 + 路签棘轮失忆 · 2026-08-03 · consulter

**派单方**：CFO。**任务 id**：`hatch-ledger`。两条一起做。

## 目标

**①** 先回答事实题：**缺 `lib/emit.sh` 时逃生口记账会不会静默停掉？**
会 → 它就不是可选件，`|| true` 是错的，升成必需件（理由＝`checks/16` 已写死的
「账本不入仓 = 所有逃生口实际上是静默的」，「记账机制自己可以静默消失」是同一个洞的另一半）。
不会 → 按铁律 23 原话补「已跳过」打印。**唯一不许的是静默跳过后报成功。**

**②** 路签棘轮失忆：`lease_widening()` 拿当前签文件当基线，签一还基线就没了，
而还签是完工的常规要求 → **跨轮次的棘轮实际不生效**，而膨胀本来就发生在不同轮次。

## 验收标准

1. ① 的结论有真路径实测支撑（不是读代码推断），两种结论都要断言 + 反向验证。
2. ② 基线取该角色**最近一次发签**的写区，跨轮次可查。
3. ② 断言必须**跨还签**点火：发窄签 → 还签 → 发宽签 → 第 9 项必须报放宽并列出签史。
4. ② 反向：把基线改回当前签文件 → 该断言必须变红。
5. ② 的 fixture 里必须有一发 **cfo 角色跨轮放宽** —— **判据别只对别人生效**。
6. 所有新增断言的真实退出码写进 worklog。

## 可触碰目录（写区）

- `scripts/`（`lib/emit.sh`、`lib/lease.sh`、`mission_start.sh`、`mission_complete.sh`、
  `arbiter-push.sh`、`run_agent.sh`、`install-gates.sh`、`selftest.d/`、`README.md`，
  以及十三个只写 diary 的脚本各一行 source 语义）
- `agents/consulter/`（worklog、判例库）

不碰：`agents/cfo/`、`code/`、已生成的模块仓。

## 自检门

- `bash scripts/selftest.sh` → exit 0、FAIL 0
- `bash scripts/gates/run-gates.sh` → exit 0
- 反向验证 ① 四发（R1–R4）、② 两发（R5–R6），**每发点亮的子断言必须不同**，
  相同就说明其中一发没在验它自己那一层
- 扰动一律在工作区**副本**里做，真工作区不动
