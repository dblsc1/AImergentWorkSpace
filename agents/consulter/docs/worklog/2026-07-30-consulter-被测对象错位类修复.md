# 2026-07-30 · consulter · 「被测对象错位」类修复（夜班，用户已休）

## 输入：CFO reviewer 子代理首轮打回——机制在工作

reviewer 常驻子代理第一轮就抓住 CFO 漏报的实体问题：「四模块重装门禁」十七个文件
全在工作区没提交，红测是真的、被测对象是错的（clone 即回事故现场而 worklog 写着已修）。
它还点破 13 号被同形状套了一层（算工作区哈希，副本没入仓照样绿）、根仓活 pre-commit
停在 306 字节旧版（停线旗插不住）、§4.1 成了假警报。**这套监督链开始自己咬住自己了。**

## 我这边的对账：同一形状我也踩了

复核发现框架仓自己的活 pre-commit 同样过期——我昨晚的停线「本仓实测」直跑的是
tracked 副本，真正守提交的活钩子没有停线逻辑。与 CFO 同日同形状。已重装并走
**真实提交路径**复测：置旗 → git commit 退 1、HEAD 未动；清旗复绿。

## 修（按类）

1. **13 号两层加固**：
   - 副本必须 `git ls-files` 可见——未入仓的门禁副本直接红并点名「clone/换机即消失」；
   - **活钩子自比**（新增，框架 checkout 不再豁免）：`.git/hooks` 里真正在跑的三个钩子
     必须与仓内 `scripts/hooks/` 字节一致——pull 只更新副本、从不更新活钩子，这层
     此前无人核。
2. **90 号取值域随 J3 迁**：worklog 说明从 `*/docs/worklog/*.md` 放宽为 `*worklog/*.md`，
   契约影响面写在新落点不再「不可见恒判未说明」（CFO 为此走的记账逃生口可平账）。
3. **断言 #46**（总 46 条）+ 判例库「第三句」：红/绿之外必问**被测对象是哪一份**；
   验门禁必须走真实路径 + 被测文件入仓。

## 验证（直取退出码）

- 13 活钩子：对本仓正向即红（抓到自己）→ 重装后绿；沙箱未入仓副本红 1 →入仓绿 0。
- 停线复测走真 git commit：置旗退 1、HEAD 未动。
- 90：契约改动无说明红 1；说明写 J3 新落点绿 0。
- selftest 46/46；#46 红/绿（改回旧取值域→红 1）。

## 给 CFO（明早 pull 后）

- 13 号会开始核你们四个模块的**活钩子**——如有过期会点名，重跑 install-gates 即平。
- 90 号平账：你那笔记账逃生口的成因已修，下一笔契约改动说明写新落点即可见。
- 你 reviewer 打回清单里剩余三条（报告未随笔更新、旧编号指代、§4.1 假警报）照你排期。

## 附：N2 机械化——「先 rebase 再装」从散文变断言（同日）

CFO 亲笔写进 worklog 的教训一轮后被自己原样违反——散文没有半衰期保证。
install-gates 装前自查源仓 vs 上游：落后即拒装（点名 N 个 commit），
AIMERGENT_INSTALL_STALE_OK=<理由> 记账放行（emit install_stale_override 事件），
离线 fetch 不通不阻断。真克隆红/绿：落后 1→拒 1；记账→放行并打印理由；同步→绿。
红测第一轮又栽在第三问（克隆拿远端旧脚本，守卫未提交）——注入后才测到真对象。
selftest #47。架构问题处理协议对用户明确：阻塞→停线旗即时找 consulter；
不阻塞→reviewer 清单攒批，consulter 定期清账。

## 附2：两笔小修（用户「别急」，本轮只此两件）

- 自决区判据措辞修正（CFO reviewer 纠正为准）：「一条命令可回收」不实——
  docker rm -f 不删具名卷；改为「可完全回收且回收命令随记账写明，容器与卷各一条」。
- 模型分档入 orchestration.md（用户裁定）：reviewer/arbiter 常规轮次（无打回）
  一律 sonnet 档，不用 opus；升档仅限打回复审与架构级裁决分析。

## 附3：障签机制（用户点破设计：过期便条该自堵，不该巡逻）

我最初提的是超时提醒（巡逻式）；用户指出该复用路签的自堵性质。落地：
blockers/ 一障一便条（必写「解除标志：<仓内路径 或 文字@文件>」），checks/15
在标志出现后变红逼销障——做完决定那笔提交不销便条就落不了地。与路签唯一差别：
签是本地态（.git/），障必须入仓（便条正是给 clone 的下一个人看的）。
report-schema：escalation != null 必须同批立障。红/绿四路（合法持有绿/标志现红/
无标志红/销障绿）+ selftest #48。模板已带 blockers/ 目录，根仓白名单已登记。

## 附4：障签接入发签时刻（用户点破：CFO 开 arbiter 前就该查便条）

起飞单新增第 8 项：blockers/ 便条带「波及：<前缀>」且与申领写区重叠 → 硬拒发签
（与写区重叠拒签同形）；无关便条仅列出提示可并行。三路实测：压线拒 1 /
无关提示 0 / 销障绿 0。aim「7 项检查单」计数化石顺手去数字化。#48 断言扩至
两时刻（着陆逼销障 + 发签拦压线），红/绿用备份副本法——本轮 git checkout 恢复
未提交文件被 harness 分类器拦下一次，判例第三次生效。

## 附5：合流 CFO 14 笔 + 清 J7 残留矛盾

rebase 零冲突。CFO 台账 J7 条目点名 supervision.md:33 仍写「consulter 改的框架
仍由 CFO 审」旧口径（与 :21 自相矛盾）——它举证、我修正，J7 全落地。
项目宪法现为唯一宪法（框架根版已由人类删除），22 条裁决俱在。

## 附6：社区对照报告入仓（用户今日理顺工作的输入件）

agents/reference/社区对照-提示词分层与报告协议.md：三子系统对照。核心发现——
①级联裁决方向社区与我们相反（就近覆盖 vs 只能加严），无人做单调加严；
②git 锚定报告与 objections 字段为真独有；③闸门三层共识（PreToolUse→commit→CI），
我们缺最早的 PreToolUse 层；④红/绿点火=手动 mutation testing，可脚本化。

## 附7：J8 规则两轨制入台账 + 今日理顺的采纳清单

用户裁决 J8：治理规则归 AGENTS.md 系列（只能加严），技术规则归 CONSTITUTION 系列
（就近优先）；重复表述改指针；exam.sh 开卷保持。另采纳待落地（用户理顺后动手）：
comm.jsonl 加因果链字段；PreToolUse 前移层（写范围 tool limit 由发任务 json 携带）；
点火脚本化。report.json 定位与 worklog/jsonl 分工答复见会话（结论：report=机器核验
的交接凭证不动，膨胀治法=证据外链，不是把详细内容搬进 jsonl）。

## 附8：文档联动/映射表保鲜对照入仓

结论三条：①docs_reviewed 表态硬拦=Changesets 范式同构（独立收敛到社区最佳实践，
多 reason 一层）；②doc-map 保鲜抄 CODEOWNERS validator 形状——glob 匹配数+路径存在+
无主目录兜底，死条目 100% 可测；③policy-as-code 无自动生成，铁律 23 手工断言即社区同形。

## 附9：团队拓扑与沟通对照入仓

三结论：①颗粒度切法=主流（契约边界+可独立验证+上下文装得下，超限拆子文件夹不加人）；
②hub 可留——损耗在转述不在中转，arbiter 禁改写+逐字证据即可；③评审建议
控制流/数据流分离：裁决经 arbiter、缺陷细节 programmer 直读 reviewreport 原文、
机械反馈自取。第③条属治理规则调整，待用户拍板后落 orchestration/角色卡。
