# 2026-07-29 · consulter · CFO 实报缺陷按类修（单文件路签 + 仓型判据第3/4例）

## 输入（CFO 上报，质量很高）

- ① 四模块补装门禁 + 裸提交红测全部通过（rc=1、HEAD 未动，双判据直取）——修复在实战闭环。
- 13 号门禁新鲜度、standing-docs 孤儿树两条断言在真实战场自己响了（先 rebase 再装的顺序坑
  也是 13 抓的）——闸门体系开始替人记账。
- 实报缺陷 A：**单文件申领不到写区路签**（norm 强加尾斜杠 × 05 前缀匹配），与白名单
  gitignore 叠加成静默丢数据路径；其 contracts/ 白名单行因此卡在工作区，clone 会缺。
- 实报缺陷 B：**checks/12、60 在模块仓跑根仓判据**（找 agents/AGENTS.md、不解析框架根），
  nginx-docker 提交被无关判据卡死——仓型类第 3、4 次出现，CFO 建议按类修。判断正确。

## 修法（按类，不按实例）

1. **公共件**：`lib/paths.sh` 新增 `repo_is_module` / `framework_root` /
   `resolves_here_or_framework`——仓型感知与框架根解析从此只有一个实现。
2. **05 + norm 双保险**：05 在前缀匹配外接受精确相等（含旧尾斜杠签，存量兼容）；
   mission_start 的 norm() 对已存在普通文件不加斜杠。
3. **12**：① 按仓型取长期文档清单（模块＝AGENTS.md+module_docs 三件+文档地图）；
   ④ 地图路径经框架根解析；⑤ 孤儿树扫描仅在有 agents/ 时跑。
4. **60**：存在性判据改为「本仓或框架根可解析」（与 check-references 同口径）。
5. **断言**：selftest #42（单文件路签）、#43（仓型公共件），总 43 条。

## 验证（全部直取退出码）

- 沙箱路签三路：单文件签内提交绿 0 / 签外红 1（点名越界）/ 旧尾斜杠签兼容绿 0。
- 真模块沙箱（new_module 全新生成）：12 绿 0、抽 handoff 红 1；
  60 引用框架路径绿 0、引用两边皆无红 1。
- #42 首轮红测没红——扰动没打中断言子串，换成抹掉机制本身后红 1/绿 0。
- **事故与恢复**：红/绿扰动后用 `git checkout --` 恢复 60，把未提交的修复一起冲掉，
  绿测假红暴露。重新补上修复并复核（grep 机制存在 + selftest 绿）。教训入判例库：
  扰动含未提交修复的文件，恢复只许用备份副本。

## 对 CFO 的答复

- pull 本次 push 后：contracts/ 白名单行可随同一逻辑变更正常落地（单文件路签已修）；
  nginx-docker 那笔的 standing-docs / doc-paths-exist 恒红消除（需重跑 install-gates
  刷新模块内 checks 副本——13 号会提醒）。
- ②交叉审区间顺延为 6cceec1..<本次 push 后 tip>。

## 用户角色澄清（记录）

consulter 主业＝构建 workspace；次业＝看 CFO 各项输出、监督编程，
**重点从输出里看架构问题**（模式层，不是逐单）。与 supervision.md 矩阵一致，无需改条文。
