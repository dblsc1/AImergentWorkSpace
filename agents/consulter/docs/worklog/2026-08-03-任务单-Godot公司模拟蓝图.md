# 任务单：Godot 公司模拟蓝图入仓（consulter，2026-08-03）

## 目标

把「治理事件流渲染成像素风编程公司」方案写成开工蓝图入仓，
供 X_structure 就绪后开干（用户裁定：先存档，暂不动工）。

## 可判定的验收标准

- `agents/reference/蓝图-Godot公司模拟-治理事件流游戏化.md` 在仓且 `git ls-files` 可见
- 含事件映射表、P0-P3 估算、MVP 一句话验收标准、P0 裁决清单（6 项待用户拍板）
- worklog + report.json 同批更新，着陆检查全绿，推上 origin/v5

## 可触碰目录

- `agents/consulter/`（自己留痕）
- `agents/reference/`（蓝图产物）
- `logs/`（账本，检查项自动放行）

## 自检门

- pre-commit 着陆检查全绿
- arbiter-push.sh 推送门通过（逃生口使用记账入仓）
