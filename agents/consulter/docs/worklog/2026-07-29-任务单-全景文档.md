# 任务单 · 工作空间全景文档

## 1. 目标
给冷启动的 agent 与人类各一份可用的全貌：角色分工、现行规矩、流程与闸门、已付学费的坑。
做成同一份，不制造第二个事实源。

## 2. 验收标准（可判定）
- [ ] `agents/reference/工作空间全景.md` 存在且 ≥200 行
- [ ] `scripts/gates/check-references.sh` 全绿（文中路径必须都可解析）
- [ ] `scripts/checks/_common/12-standing-docs.sh` 全绿
- [ ] 文档地图列出本文，且 consulter 的 system prompt 指向它

## 3. 允许触碰的目录
- 可写：`agents/reference/`、`agents/consulter/`、`.claude/agents/`
- 只读：其余
- 禁碰：`code/`、CFO 的项目树

## 4. 应代码化的验收项
路径可解析、长期文档体检 —— 已由 checks 60 / 12 覆盖，无需额外脚本。

## 5. 交付前必过的自检门
`scripts/gates/check-references.sh` + `scripts/checks/_common/12-standing-docs.sh` + `scripts/selftest.sh`

---
tier: normal
