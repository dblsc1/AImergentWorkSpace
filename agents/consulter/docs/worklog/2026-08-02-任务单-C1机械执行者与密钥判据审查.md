# 2026-08-02 · CFO 派单 → consulter · 三件事

## 目标

补上 C1（单文件行数）的机械执行者，并独立审核 CFO 写的密钥判据。
上位目标见 `agents/reference/用户目标口述-可复现工作机制.md`：
散文规矩没有执行者 = 限制不住 vibecoding。

## ① 审 CFO 写的密钥判据（只报告，不修）

`scripts/checks/_common/18-secret-literal.sh` + `selftest.sh` #61（commit b6bb516）。
CFO 明确要求按审别人的标准审，重点：漏网面 / 误报面 / 判据能否被静默绕过。
**红线：发现问题写报告，绝不顺手修。**

## ② C1 执行：拆 selftest.sh

CFO 裁决：下一轮、单独一条 commit、零行为改动。
验收：拆完 `selftest.sh` 仍是唯一入口，PASS/FAIL 计数与用例编号不变
（基线 PASS 61 · FAIL 0 · N/A 0，exit 0）。

## ③ 给 C1 补机械执行者

三档照 C1 原文：≤500 常态 / 500–1000 必须在 report.json 记一笔（判据要能核到那一笔）
/ >1000 硬拦。认 D2 存量导入例外。**统一一个机制**，不新造第二套。

## 可触碰目录

`scripts/selftest.sh`、`scripts/selftest.d/`、`scripts/gates/run-gates.sh`、
`agents/consulter/docs/`

## 自检门

`scripts/mission_complete.sh` 全绿；每条新断言必须红/绿反向验证并把真实退出码写进留痕。
