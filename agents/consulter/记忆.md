# consulter 判例库（已付过学费的坑）

> 每条都来自本项目或前身项目的真实事故。审核时对照；新事故往下追加，不删旧条。

## 完整性类

- **假绿最危险**：门禁退 0 ≠ 验过。check-report-schema 曾在空区间上零验证报绿，
  掩护了 10 个 commit 直提 main。**必须看到它打印被验对象**。
- **留痕落点三杀**：目录不存在（agent 就不写）、写在 /tmp（会话结束蒸发，9 份报告全灭）、
  被白名单 gitignore 静默吞（写了 = 没写，踩过 4 次）。核留痕先核落点。
- **撒谎式成功**：脚本写产物不验落点，被吞了照常打印 ✅。见 emit.sh 的 assert_trackable。
- **口头完成不认**：Codex 曾报"迁移好了"，实际核心任务整个没做。核 commit SHA、核 remote tip。

## 修复质量类

- **修类不修实例（铁律 23）**：A1 占位符自毁、install-gates 装错目录、merge 脚本引用死路径,
  全是同一形状——字符串替换式改动,静态 grep 看不出"引用物已不存在"。修完必须留断言,
  否则下次重构原样复活。
- **半做比不做危险**：GAP-1.2 只改了一份契约,另一份留着相反规则,下游以为已对齐。
  裁决落地必须全量清扫 + grep 反查留痕。

## 认知偏差类（审 AI 的产出时专用）

- **过度自责掩盖框架缺陷**：自责 ≠ 归因。arbiter 曾把框架缺陷全揽进自己检讨,
  显得负责,代价是下一个人继续踩。审报告时问:这条"违规"的根因真在执行者吗?
- **确认即停**：读到符合假设的证据就停止阅读,三次事故(A1-5 正则、--hook-only 顾虑、验收误扫)。
  下断言前先跑一遍看**全部**命中。
- **自我豁免**："根仓不算模块"、"契约是特例所以升档"——为想做的事发明规范外例外,
  一个模式能生成无限条违规。
- **指令无人审**：执行者产出有 reviewer,编排者的任务单没人看,错误率反而更高
  (一轮四次被执行者纠错,零次被机制抓到)。审 arbiter 时重点看任务单质量与 objections 记录。

## 流程类

- **先审后推**：审绑本地 commit(不可变、有 SHA,足够);被打回的活不上远端。
- **续用 > 重开**：打回循环重开 agent = 冷启动重读全部上下文,实测占一轮消耗 31%。
- **判断题不做硬闸门**：硬闸门只配事实题;判断题硬拦会逼人用逃生口,逃生口用滥整套闸门就废。
- **逃生口必须记账**：override 可以用,但必须在 diary 可见。审时查 override 次数与理由。
- **框架修改回框架仓**（GitHub v5 分支）,不在 CFO 工作树里动手;实例修复才在工作树。

## 字面量类（CFO 2026-07-29 提炼，第三次复发才看清）

**凡是「报告一个关于字面量的缺陷」的文档，都会成为那个缺陷的新实例。**

实证三次：占位符 token 先后出现在①模板检查脚本自身（被 replace_token 吞掉）、
②heredoc 展开、③上报该缺陷的 report.json 与 worklog 里（逐字引用）。
每次都以为修的是"那一处"，其实是这个形状换了个载体。

**判据**：讨论 token / 占位符 / 敏感字面量的**文档**，与**检查脚本本身**是同一条规矩，不是两条。

## 不重复造轮子（2026-07-30 用户裁定，预研实证后立为总则）

**遇到设计/流程问题，先查前人成果，再决定自造。** 已实证：本框架大部分构件
（角色分层、结构化交接、审核代码化、判例库）都有被验证过的先例，自我怀疑和
重新论证它们是纯浪费；而「pre-commit 过重会被绕过」这类坑，别人已经付过学费。
对照报告：`agents/reference/前人成果对照-多agent框架.md`（含来源链接）。

**参考性强的项目（按用途查）**：
- **Devin**（Playbooks/Knowledge/多实例管理）——运营形态最像本框架，检查单与记忆机制先抄它
- **OpenHands**（开源，事件流架构）——jsonl 留痕/事件字段设计对照它
- **MetaGPT**（SOP 结构化交接）——角色间只交结构化产物的始祖
- **AgentCoder**——测试设计者与实现者分离的实证依据（J2 的靠山）
- **agentic-os**（无证据不算完成）——铁律 18 的独立重发明，证据流程对照
- **Microsoft Agent Governance Toolkit**——commit→PR→CI 三层闸的工业化参照

**判据**：想造新轮子前必答两问——①这问题在上述项目里怎么解的？②我们的场景哪里
不同到非自造不可？答不出第二问就用现成的。真空白（lease/停线旗/新鲜度比对/逃生口
记账）才值得自造，且自造件要主动找替代方案对比论证（如 lease vs worktree）。

## 仓型与路签类（2026-07-29 CFO 实报后按类修）

- **判据仓型不匹配已四次实证**（run-gates 装进模块恒红、mission_start#4、checks/12、checks/60）。
  修法已抽公共件：`lib/paths.sh` 的 `repo_is_module` / `framework_root` / `resolves_here_or_framework`。
  **新写任何 check 先问「我在哪种仓」，一律用公共件，不再各自手写仓型判断**（selftest#43 盯着）。
- **单文件申领不到路签**：norm() 强加尾斜杠 + 05 前缀匹配 → `.gitignore` 永远匹配不上；
  与白名单 gitignore 叠加＝静默丢数据路径（白名单行加不上→新根级目录被吞）。
  修法双保险：norm 对已存在文件不加斜杠 + 05 接受精确相等。**通用形状**：
  「前缀匹配」类判据必须自问——单文件、精确相等这两种输入它认不认。
- **红/绿扰动前先看工作区**：本轮用 `git checkout --` 恢复被扰动的文件，把**未提交的修复**
  一起冲掉了（绿测因此假红才发现）。扰动含未提交修复的文件，恢复只许用备份副本，不许用 git。
写法一律拆开：`'{{'"NAME"'}}'`、或转义、或只描述不逐字引用。

**审的时候怎么用**：看到一份文档在讲某个字面量，先问「这份文档自己算不算那个字面量的实例」。

## 恒定答案类

**一条永远给同一个答案的检查，等于没有这道门。** 已实证五种形态：
- 空区间假绿：`merge-base == head` 时 diff 为空，零份验证却退 0
- landed 恒真：快照含脚本自己的 diary 写入，写一行就算"活落盘了"
- 模块门禁恒红：`run-gates` 引用的 `check-references.sh` 没被装进模块
- 判据仓型不匹配：照框架仓写的检查装进模块仓，恒红
- **调用方漏传环境变量，子检查恒放行**（2026-07-31）：`mission_complete.sh` 推断出
  `role` 后从未 `export AIMERGENT_ROLE`，`checks/_common/05-write-lease.sh` 读到空
  值就触发「角色未知不拦」的兜底提前 `exit 0`——常规提交路径（不手动传变量）下
  写区路签检查形同虚设，退 0 和真验过在界面上完全一样。**这个形状的特征**：兜底分支
  本意覆盖"真不知道"，实际吞掉了"知道但没传"；光靠 grep 源码里有没有 `export`
  字样验不出来（判例库「字面量类」同源教训），必须在沙箱里真放一个越区文件、
  不设环境变量、断言退出码非零。修复见 `scripts/mission_complete.sh`（`role` 定案后
  立即 export）+ `scripts/selftest.sh` #50（沙箱 mktemp 仓，红测 pre-fix commit
  6619b35 exit=0 静默放行，green 测工作区 exit=1 真拦住）。

**审的时候怎么用**：对任何一条检查，追问两句 ——
**它在什么情况下会永远通过？在什么情况下会永远失败？** 两个答案都必须是"没有这种情况"。
反向验证（放入违规→变红，拿走→复绿）是唯一可信的证明，看代码不算。

**红/绿之外还要问第三句：被测对象是哪一份？**（2026-07-30，两任同日各踩一次）
- CFO：红测「四模块门禁」全过——被测的是**没入仓的工作区副本**，clone 即回事故现场。
- consulter：红测「停线旗拦提交」全过——直跑的是 tracked 副本，而真正守提交的
  **活钩子（.git/hooks）还是旧版**：pull 更新仓内副本，从不更新活钩子。
判据：验门禁必须走**真实路径**（真 git commit，不是直跑脚本），且被测文件必须
`git ls-files` 可见。13 号现在两层都核（入仓 + 活钩子自比，框架 checkout 不豁免）。

**这条从今天起是硬要求，不只是审法**（2026-07-29，两任 consulter 共识）：
**新增任何断言/闸门/检查，同一逻辑变更里必须做一次红/绿反向验证并把真实退出码写进留痕。**
只写断言不点火 = 又一台没验过的烟雾报警器。已实证收益：#36 计数断言的红测当场
抓住了并行会话里全景文档的硬编码「23 条」。

**修对了不等于诊断对了（2026-08-02，F7 实证）**：`comm: not in sorted order` 那条噪音，
我判的成因是「本仓路径大量含非 ASCII，sort 走 locale 排序」。修法（`LC_ALL=C`）是对的，
**成因是错的**——实测中文路径 byte 序与 locale 序相同、根本不触发；真正触发的是
en_US.UTF-8 的另外两条规则：**忽略前导标点**（`.gitignore` vs `code/x`）与  <!-- ref-fixture -->
**不分大小写**（`scripts/README.md` vs `scripts/gates/a.sh`）。  <!-- ref-fixture -->
**直接后果**：照错误诊断写出的断言 fixture 用了中文文件名，
拿掉 `LC_ALL=C` 做红测时**它没变红**——那是一台在验不存在机制的烟雾报警器，
只有「每条断言都必须反向验证」这条硬要求才把它逼出来。
**判据**：修复通过 ≠ 诊断成立。诊断写进注释和判例库之前，
先问「按我这个成因，最小复现是什么」，然后真去跑那个最小复现。
（**这一条自己就踩了一次「字面量类」**：上面那行逐字引用的示例路径按定义不存在，
checks/60 当场判它是死链——已用行尾 ref-fixture 标记，与 check-references 同一个约定。）

**测试 fixture 路径是一整类，别逐条登记豁免（2026-08-02）**：
喂给路径判据的合成输入（selftest 里那些 `code/<子文件夹>/worklog/` 之类）
会被 `check-references` 全数误报成悬空引用。它们按定义就不该存在——
要它们真存在才能过，等于要求 selftest 往仓里种垃圾。
逐条登记进 `doc-path-exempt.txt` 只修实例，而且那张表会慢慢长成一份混着
「真的还没建」与「按定义永远不会建」两种东西的名单，再也分不开。
修法是**行级** `ref-fixture` 标记（`gates/check-references.sh`）。
**配套铁则**：任何豁免机制落地时，必须同时验它**没顺手把整道门关掉**——
selftest #57 的第二发红测（把行级豁免改成文件级）比第一发重要。

**机制会诱导它想防的行为（2026-08-02，F2 实证）**：
写区路签的棘轮不是「谁粗心」——05 拦下越区提交后，**放宽自己的签是最省事的出路**，
所以每次膨胀前 90 秒内都紧跟一条 `failed:write-lease`。
落地棘轮警告之后的第一分钟，它就拦下了**它作者本人**的一次放宽
（申领 `scripts/ agents/` → 报「放宽：agents/consulter/ → agents/」）。
**判据**：看到一个反复犯的错，先问「现有机制是不是让犯它比不犯它更省事」，
再决定是加提醒还是改机制。

**验证者自己的 $? 也会撒谎（同日实证三次）**：`cmd | tail; echo $?` 取到的是管道
末段的退出码——曾把真修复看成无效、把有效门禁看成恒绿、把被拒的 commit 看成已提交。
判退出码必须直取（`cmd >f 2>&1; echo $?`）或 `PIPESTATUS[0]`；
判 commit 成败只认 `git log`/`rev-parse`，不认回显。

## 恒绿的门比缺门更贵（2026-08-02，C1 实证）

CFO 报「C1（单文件行数）从落地起一直是散文，没有执行者」。**实测是：执行者一直在，
一直打印 ✅「无超限文件」**，而它从没看过 1129 行的 `selftest.sh`。两个**互相独立**
的病根，任一个单独存在就足以让它全瞎：
- `tracked` 用 `[ -d code ]` 判仓型 —— 框架仓的 `code/` 也存在（装的是各自独立的模块仓，
  `git ls-files` 本来就看不见），于是只扫 `code/_template` 下 26 个文件（全仓 259）
- 扩展名白名单只认 `py js ts html css vue svelte` —— `.sh` `.md` `.json` 全不看

**判据**：审任何一条「我们有这个 gate」的说法，第一步不是找脚本，
是问**「它上次打印的被验对象是什么」**。「缺一道门」和「有一道恒绿的门」在待办清单上
长得一样，但后者更贵 —— 它让所有人相信这件事已经被管住了。
配套硬要求：**任何 gate 必须打印它扫了多少、跳过多少**，只印一个 ✅ 就是下一次哑四天的种子。

（同一个 `tracked` 还喂着「禁默认值」「禁止路径」两道 gate，在框架仓同样只看那 26 个文件 ——
修 `tracked` 是修类。这也是「判据仓型不匹配」的**第五次**实证，公共件 `repo_is_module`
早就在 `lib/paths.sh` 里，只是没人用。）

## 共享库一缺，半套门禁静默放行（2026-08-02，P0）

删掉 `scripts/lib/paths.sh`，暂存 251 个文件跑着陆检查单：**十一条 `_common` check 里
九条静默 `exit 0`，界面上和真验过一模一样**（含 `secret-literal`、`report-committed`、
`doc-paths-exist`、`reviewer-opinion`、`dependency-drift`）。

机制三段，缺一不可：① check 只 `set -uo pipefail`，**没有 `-e`**，source 失败不中止；
② `staged_paths` 未定义 → `mapfile` 读到空 → 文件数组为空 → 走到 `exit 0`；
③ `mission_complete.sh` 写的是 `if out=$("$c" 2>&1); then cl_ok`，
**`$out` 里的 `command not found` 在成功分支被整个丢弃**，一个字都不显示。

铁律 23 早就点名了这个形状（「关键路径缺文件必须 die」），只是当时的例子是
`if [ -x <脚本> ]; then … fi`，没人想到 `. <公共件>` 是同一个形状。
**判据**：`.`／`source` 一个共享库，与 `if [ -x ]` 是同一条规矩，不是两条。
凡 source 必须 `|| exit 2`，凡「拿不到输入就退 0」的 check 必须先问
「输入为空是因为真没有，还是因为我瞎了」。

## 「字面量类」第四次复发：这次载体是审核报告本身（2026-08-02）

判例库那条已实证三次（模板检查脚本 / heredoc / 上报缺陷的 report.json）。
**第四次的载体是「审密钥判据的审核报告」** —— findings/report.json 与 worklog 逐字
引用了十几种明文凭据样本，`checks/18` 当场把提交拦下。

两个新增细节：
- **空格拆不开**：判据的正则是 `(_SECRET)[[:space:]]*[:=]`，在名字和 `=` 之间插空格无效。
  有效写法只有三种：反引号拆（`` `NAME` `` + `` `=值` ``）、shell 里相邻单引号拼
  （`'NAME''=值'`）、或干脆只描述不逐字引用。
- **别用逃生口**：这次的正确反应是改写法。而这恰好证明了另一件事 ——
  **误报可不可接受，取决于「改写法这条路存不存在且明不明确」**。
  判例库写了那条路，所以我没绕；撞上 `code/_template` 那处误报的人没有第二条路
  （改模板越权），就只剩逃生口。评估任何误报，先问「被误伤的人还有别的出路吗」。

## 反向验证第三次验出验证者自己（2026-08-02）

- 给 #62 写的子断言 `case "$out" in *"扫描 "*)` 是**恒真**的：
  `sed -n '/单文件行数/,/gitleaks/p'` 的**末行**是「── gate: gitleaks 密钥扫描 ──」，
  自带「扫描 」。删掉被验的打印行照样绿。
  **判据**：用 `sed` 区间取输出时，区间的**首行与末行属于别人**，
  子断言必须钉在只有被验行才有的字样上。
- 给 fixture 贴 `# ref-fixture` 时贴到了**续行 `\` 后面**：反斜杠转义掉注释前的空格、
  `#` 吃掉换行，下一行 `> file` 变成一条**只有重定向**的命令，把文件截成空 ——
  **而 `bash -n` 报 OK**。语法检查查不出语义塌陷。

## 修对了不等于诊断对了（第二例，同日）

CFO 立项时写「gitleaks 配置是空壳零规则」。实际全文是 `[extend] useDefault = true`，
即**继承上游一百多条默认规则**；而且 CI 里 `gitleaks-action@v3` 一直在跑。
真实失效只有本地两条（开关默认 0 + 本机没装二进制）。

**代价是具体的**：按错诊断，下一步是「往自己的判据里塞更多正则」；
按真诊断，下一步是「本地把 gitleaks 装上」——而自写判据漏掉的
AWS 密钥 / GitHub PAT / PEM 私钥**恰好全在 gitleaks 默认规则里**。
两条路成本差一个数量级。**呼应「不重复造轮子」：自造前先确认现成的那个是真的坏了。**
