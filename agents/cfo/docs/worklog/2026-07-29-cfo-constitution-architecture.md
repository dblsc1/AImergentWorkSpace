# worklog · 宪法填实：事件标准的四条架构裁决

- 日期：2026-07-29
- 角色：CFO
- tier：`normal`

## 背景

人类要做事件信封（yq-event/v1）的架构决策，但表示"记不清结构了"。
我按局外领导的角度做了一次完整交底（目标 / 路径 / 结构），并把四处含糊点摆成决策题。
含糊本身不要紧，**要紧的是含糊的东西一旦被实现出来就固化了**——
实现者会替人类选一个，而且不会告诉他自己选了。

## 四条裁决

| # | 裁决 | 谁定的 |
|---|---|---|
| D6 | 跨模块契约落仓根 `contracts/` | 人类（方案 A） |
| D7 | 防重按 `dedupeKey`，与 `id` 是两个不同的东西 | 人类 |
| D8 | 双时间：`time`（发生，给用户看）+ `recordedAt`（收到，服务端盖章） | 人类 |
| D9 | `subject` 分层：`tier2`/`tier3` 必填分类词表，`project`/`task` 永远选填 | 人类委托 CFO 查通行经验后定，人类采纳 |
| D10 | `flags` 暂行任何来源可贴 | 人类 |

## D9 的依据（人类明确要求"找通行经验"，不接受我拍脑袋）

三条站得住的行业实践：

1. **事件的每个字段自第一天起即视为公开 API**——改名/删字段都要迁移方案。推论：字段越少越好。
2. **归一化 vs 反归一化取决于耦合度**：内部紧耦合用 ID（改名安全、可 join）；
   外部松耦合用副本（对方拿不到你的 ID）。
3. **CloudEvents 的 `subject` 是给中间件做过滤路由的**，不是塞领域对象的地方。

撞进本项目就出答案了：我们**同时有两类消费方且需求相反**——
投影/视图要 ID（项目改名不能污染历史），学生脚本**根本拿不到我们的 ID**（他只能写"学习"/"英语"）。
`subject` 现在混着，根源就是用一个结构服务两类需求。

**解法是承认它们是两种东西**：`tier2`/`tier3` 重新定性为**受控分类词表**（不是分区/项目的显示名），
`project`/`task` 降为选填的内部 ID。要害在于：分类标签是**它自己的事实，不是别人的副本**，
所以分区改名怎么改都不触碰历史。而且这本来就符合花园模型——树按 tier2+tier3 长，
**分类轴本来就是一等领域概念**。

## D8 的正式名字

人类说"time 这是个坑，需要前端时间和后端时间戳"——这个直觉对应的正式做法叫**双时间（bitemporal）**。
分开之后两件事同时成立：学生补交昨天的记录按 `time` 长在昨天；重放排序按 `recordedAt` 不受补交扰动。
**合成一个字段就必然牺牲其中一件。**

## 顺手发现的契约自相矛盾

HANDOFF §3.1 写「`id` + `source` 构成防重键」，而 §6 的唯一索引是 `(user, source, dedupeKey)`。
**同一份文档里两处对不上**，而且没说 `dedupeKey` 是不是就是 `id`。
D7 把它定死了，并登记进 §4.1 待改清单。

## §4.1 现在非空——这是有意的

五条裁决全部标「未落地」，因为 `contracts/` 还没建、`yq-event-v1.md` 还没写。
按宪法 §4.1 的规矩，**下一轮的第一件事是清空它，不得先做新功能**。
这一节存在的意义就是防止"人批了、契约没跟上、agent 照旧契约实现出相反的东西"。

## 依赖漂移说明（检查项 90）

无新增/删除包依赖。未改动任何现有契约文件（`contracts/` 尚未建立）。
本次是宪法与裁决台账的填实，不触碰任何模块代码。

## 未完成（下一轮第一件事）

清空 §4.1：建 `contracts/` + `.gitignore` 白名单 + 写 `contracts/yq-event-v1.md`
+ 同步 HANDOFF 与文档地图。

---

## 追加（同日）：宪法定性修正 + mock 验收

人类删除了框架根 `CONSTITUTION.md`，`agents/CONSTITUTION.md` 成为唯一宪法（D13）。
标题去掉「加严层」，定位改成本项目唯一权威。HANDOFF 降级为**讨论草稿**，顶部加声明。

行数改为分档（D12）：≤500 建议 / >500 记 report / >1000 必须分拆。
这与 D2（index.html 1280 行不硬拆）冲突，人类裁决 **D2 保留**（D14），
在 C1 里写成存量导入例外，并要求派生时在文件顶部写「下次重构拆掉」。
**注意：那句话写在派生件上，不写进保险柜** —— 保险柜是只读快照，
改一个字节 SHA256 就对不上，指纹作废。

### mock 独立验收（不采信它自己的测试）

起真服务打真请求：占比 `47.5`/`32.1`（0–100 ✅）、空闲态四字段全 `null` 且 `running:false` ✅、
默认 8 normal / 0 ephemeral 且 `?includeEphemeral=true` 才出现 3 个 ✅、
项目字段无 `color`/`icon` ✅、`NEXUS_BIND` 默认 `127.0.0.1:8000` 非 `0.0.0.0` ✅、
`?mockState=bogus` → 422 不静默回退 ✅、代码只在 `views/` 子边界内 ✅、最大文件 258 行 ✅。

**它做得比我要求的好**：把 R3 反向验证推广成 69 条用例的自测
（48 条违规输入必须变红，全部如期变红）。我只在一处提过这个思路，它做成了体系。

一处是我自己搞错的：先用 `?state=idle` 试被静默忽略，一度以为是缺陷——
参数名是 `mockState`，未知查询参数被忽略是 FastAPI 默认行为。

免模块 reviewer 一轮由人类裁决（D15），**符合铁律 19**：
免掉的审核由已入仓的确定性脚本顶上，不是"我跑了几条命令核过了"。
