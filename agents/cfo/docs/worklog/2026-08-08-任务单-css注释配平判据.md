# 任务单 · CSS 注释配平判据（CFO 自领，铁律 23）

- 角色：cfo · 日期：2026-08-08 · tier：simple

## 目标

auth 夜班实证：兜底 tokens 块注释里的 `--plan*/--fact*` 提前闭合注释，
**整个 light 主题 :root 块被浏览器静默丢弃**——无报错、页面照常渲染。
四个前端仓按同一契约写同款长注释兜底块，这是类不是实例。
修类：①已向在飞的 nginx-docker/ring/table/gantt 四个 agent 广播自查令；
②新增提交时刻机械判据；③selftest 断言令其不可退化。

## 可触碰目录

`scripts/checks/_common/`（新增 19）、`scripts/selftest.d/`（断言 70）、`agents/cfo/docs/`。

## 验收标准与自检门

- [x] 19-css-comment-balance.sh：配平放行 / 失衡点名（含文件名与开闭计数）/ --describe /
      缺 paths.sh 响亮死；--files 测试口供 selftest 喂 fixture
- [x] 反向验证双向点火：ok.css exit 0，bad.css exit 1（真实输出已核）
- [x] selftest 断言 70：坏样本拆分书写（fixture 不以完整形态入仓）；全量 70 PASS 0 FAIL
