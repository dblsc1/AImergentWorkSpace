# 任务单 · 夜班 M1：design-tokens-v1 契约 + 派活（CFO 自领）

- 角色：cfo · 日期：2026-08-08 · tier：simple
- 授权：用户「批准，去做吧。夜班。问题自己决定。」——PRD 已批，夜班自决权生效。

## 目标

M1 契约先行落地：`contracts/design-tokens-v1.md` v1.0（token 表 + 对比度门限 + 注入/兜底行为），
随后按 PRD §12 向六模块发任务单并行推进 M2/M3。

## 夜班裁决（记档）

1. **预设只换 `--accent*` 机身色族**，`--plan*`/`--fact*` 台账语义色固定——
   比 PRD 原文「每套预设整块换」收敛；理由：台账色是信息不是装饰，整块换会造成
   amber 预设 vs 琥珀警示、violet 预设 vs 计划紫的语义碰撞（契约 §2 记档）。
2. O3 色值收口：全组合实算过门限（contrast.py，会话 scratchpad）。
   **抓到设计稿 v1 自身错值**：事实青亮 #0E8C7F 实测 4.14 < 4.5（头注释写的 4.6 是估算），
   压暗为 #0B7A6F（5.21）；s2 同修（#55AFA3→#37948A）。契约 §3.2 留修正记录。
3. 契约落点：root `contracts/`（先例 yq-event-v1），PRD 原引 `agents/contracts/` 为笔误，
   已改；doc-path-exempt.txt 里的前向豁免删除（文件已真实存在）。

## 可触碰目录

`contracts/`、`agents/cfo/docs/`、`scripts/gates/doc-path-exempt.txt`（删一条豁免）。
不碰模块 code/、review/。

## 验收标准与自检门

- [x] 契约含：三轴/语义规则/token 全表（3 预设 × 亮暗）/对比度实测+门限/A1-A3 口径/变更流程
- [x] 全部承诺对比度实算 ≥ 门限（contrast.py 输出 ALL PASS 后方可入表；4 处修正即由它抓出）
- [x] tokens.css 种子与契约同步（--fact 修正 + --accent 三预设块）
- [x] PRD 引用路径与豁免文件一致性（grep 双向核）
