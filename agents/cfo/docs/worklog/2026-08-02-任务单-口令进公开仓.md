# 2026-08-02 · CFO 自派 · 我把口令写进了公开仓

## 目标

清掉当前树里的明文，并如实记录：这是「字面量类」第四次复发，且这次在**公开仓**。

## 验收标准

- 公开根仓当前树 `git grep` 零命中
- 记清历史里删不掉的事实与它对轮换决定的影响

## 可触碰目录

`scripts/checks/_common/18-secret-literal.sh`、`agents/cfo/docs/`、`logs/`

## 自检门

`scripts/mission_complete.sh` 全绿。
