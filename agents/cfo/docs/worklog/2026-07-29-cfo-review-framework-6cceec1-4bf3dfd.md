# 辅助举证 · consulter 框架区间 `6cceec1..4bf3dfd`（13 commit）

- 日期：2026-07-29
- 举证者：CFO
- 审查者：**人类**（裁决 J6：consulter 的框架改动由人类审，CFO 辅助举证、不下 verdict）
- 区间：`6cceec1..4bf3dfd`，53 文件 / +1372 −119

> **编号注记（2026-07-30 补）**：本文写作时该裁决编号为 **J6**；后因与 consulter 同日写入的
> 另一条 J6 撞号，**本文所指的那条已让号为 J7**。正文保留原样不改写——worklog 是历史叙事，
> 改写它等于篡改留痕。读到本文里的 J6 时请按 J7 理解。

> **本文不是 verdict。** 下面全部是**可复跑的实跑证据**与我的建议，
> 结论由人类下。我按角色卡的三件审法逐件跑了一遍，把结果摆出来。
>
> 口径说明：`agents/protocol/supervision.md` 第 21 行目前仍写「consulter 框架改动 → CFO 交叉审」，
> 与 J6 相反，已登记进宪法 §4.1 待改清单。**本文按 J6 的新口径写。**

## 审法（角色卡三件，逐件实跑）

### 一、门禁是否真的会响 —— ✅ 通过

**没信脚本打印，自己放违规进去看拦不拦。**

`checks/_common/14-handoff-fresh.sh`（本区间新增）红/绿点火：

| 火 | 构造 | rc | 结果 |
|---|---|---|---|
| 红 | 改 `code/backend/app/config.py`，不动 `handoff.md` | **1** | ✅ 拦住，且报错指出「一页纸是必备件」 |
| 绿 | 同批补上 `code/backend/handoff.md` | **0** | ✅ 放行 |

`checks/_common/10-worklog-changed.sh` 判据从 `*docs/worklog/*.md` 泛化成
「路径里有 `worklog/` 目录段」——J3 把留痕迁到代码旁之后，
`module_docs/worklog/` 与 `code/<子文件夹>/worklog/` 都能认。泛化方向正确，未削弱判据。

`mission_start.sh` 起飞单第 4 项修好了我上一轮报的缺陷：角色卡查找从只认
`codeagent/$role/AGENTS.md` 改为回落 `agents/$role/AGENTS.md`，
项目级角色（cfo / consulter）不再永远过不了。**已实跑确认 rc=0。**

### 二、是否修到类（铁律 23）—— ❌ **未修完（我建议在此打回）**

区间内 `cbce175` 修的正是「**模块仓在跑根仓判据**」这个形状
（起飞单第 4 项只认模块级角色卡）。按铁律 23，**碰了这个形状就有义务扫同类**。

同类没扫干净。在 `code/nginx-docker` 实跑：

| 检查项 | rc | 现象 |
|---|---|---|
| `12-standing-docs.sh` | **1** | 要求 `agents/AGENTS.md`、`agents/reference/manual/文档地图.md`、`scripts/README.md` —— 这三份是**项目级**文档，住框架根，模块仓里当然没有 |
| `60-doc-paths-exist.sh` | **1** | `module_docs/rules.md` 引用 `agents/reference/upstream`（保险柜）——**这个引用是对的**，模块的静态原件确实来自框架根，但检查从模块仓内解析不到 |

**证据表明这个模式 consulter 是知道的**：同区间新增的 `14-handoff-fresh.sh` 第 5 行就写着
`[ -d codeagent ] && [ -d module_docs ] || exit 0   # 非模块仓（如框架仓）不适用`。
`11` / `13` / `20` / `70` 也都有仓类型判断。**只有该有的那两条没有。**

清点全部 `_common` 检查项：

- 有仓类型判断：`11` `13` `14` `20` `70`
- 无、但**本来就与仓类型无关**（路签 / 有无 worklog / 分支名 / 依赖漂移）：`05` `10` `40` `90`
- **无、且正是失败的那两条**：`12` `60`

**这不是理论问题**：`code/nginx-docker` 本机网关那笔提交此刻就卡在这两条上，
而那笔改动本身完全合规。

### 三、selftest 是否跟着加断言 —— ✅ 通过（但见下方一处保留）

`./scripts/selftest.sh` → **rc=0，PASS 41 · FAIL 0**。区间新增 7 条（35–41），
逐条都对应一个真实事故，不是凑数：

- 35 监督分工散写三处、改一处漏两处
- 36 「N 条铁律」的 N 是化石（实证同仓同时存在 12/22/23 三个数字，实际 35 条）
- 37 起飞单第 4 项只认模块级角色卡 ← **我上一轮报的那条，有断言了**
- 38 J3 布局三层同时认新落点
- 39 一页纸「每改必核」
- 40 实例模型 + session 复用落点
- 41 生成的子代理 prompt 不随源再生就会漂

**36 这条尤其好**：把「写下来的数字会漂」本身做成断言，
这是从一类错误里提炼出通则，正是铁律 23 想要的形状。

**保留**：第二件的两个漏网点**没有对应断言**。按铁律 23「没有断言的修复不算修复」，
即使补上 12/60，也必须同时加一条 selftest 断言（例如：任一 `_common` 检查项在
模块仓内空跑不得因为缺项目级文档而红），否则下次重构会原样复活。

## 我建议打回的一条（供人类裁决）

**F1 · 「模块仓跑根仓判据」这个类没扫完，剩 `12-standing-docs` 与 `60-doc-paths-exist`。**

- 修法与 `14` 已经用的一致：先判仓类型，模块仓内跳过项目级文档要求。
- `60` 还需要能解析**跨仓引用**：模块合法地引用框架根路径，
  模块内有 `.aimergent-framework` 记着相对位置（内容 `../..`），可据此解析。
- 顺带：`doc-path-exempt.txt` 落点在框架根，模块仓里没有，
  所以模块内的有意前向引用（如 `code/backend/docker/docker-compose.yml`）**无处登记**。
- **必须配一条 selftest 断言**，否则按铁律 23 不算修完。

## 顺带发现（不构成打回，但要记账）

**G1 · `review_complete.sh` 的角色白名单不含 `cfo`。**
脚本只认 `programmer_reviewer|module_reviewer|consulter`。
在 J6 之前这是缺陷（矩阵指定 CFO 审却落不了痕）；J6 之后 CFO 不再下 verdict，
**这一格自动消解**——但矩阵第 21 行的旧文字仍在，两处口径不一致本身要清（见 §4.1）。
记在这里是因为：它属于「角色模型与实际层级对不上」这个类，
和 F1、和之前四次是同一个根因家族，值得一起看。

**G2 · 单文件申领不到写区路签**（本轮已在我的 report 里上报）：
`mission_start.sh` 的 `norm()` 把 `.gitignore` 归一成 `.gitignore/`，
`checks/05` 验签永远匹配不上。后果是根级单文件无法合法进入任何提交，
`contracts/` 的 `!/contracts/` 白名单因此至今没能入仓——
**而白名单不加，新根级目录会被 `.gitignore` 静默吞掉**。两个缺陷叠起来是一条静默丢数据的路径。

## 结论

区间的**主体工作质量高**：J2–J5 落地完整，新增断言条条对应真实事故，
`14` 的红绿点火我实跑通过，我上一轮报的角色卡缺陷确实修了并配了断言。

**但按角色卡审法第二件（修到类），本区间未通过** ——
它自己碰了「模块仓 vs 根仓」这个形状，也在新代码里用对了模式，却没回头扫同类，
留下的两条此刻正在阻塞一笔合规的模块提交。

**我的建议：打回 F1**（含配套 selftest 断言），G1 / G2 同批处理——它们是同一个根因家族。
**是否打回由人类定**，本文只举证。

> 未顺手代修：框架归 consulter。
