# 2026-08-03 · CFO 自派 · 修我自己引进的 exam.sh 回归
## 目标
上一轮修 exam.sh 时调了一个全仓从未定义的函数，模块里必错；我的断言只在框架根跑，测不出来。
## 验收标准
模块独立仓里考试能读到 schema；答具体路径与答模板都算对；断言覆盖模块场景且反向验证真变红。
## 可触碰目录
`scripts/exam.sh`、`scripts/selftest.d/`、`agents/cfo/`、`logs/`
## 自检门
`selftest.sh` 零 FAIL + `mission_complete.sh` 全绿。
