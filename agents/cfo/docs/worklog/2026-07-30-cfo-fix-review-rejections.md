# worklog · 修 cfo_reviewer 第 1 轮打回的三条

- 日期：2026-07-30
- 角色：CFO
- tier：`normal`

## 打回理由与处置

### R1 · 报告没随最后两笔更新（铁律 12/18）

`report.json` 的 `resolved_head` 停在两笔之前，`task` 还写着上上轮的事。
让号那份 worklog 自己写 `tier: simple` ＝「只需 report + commit」，然后没交 report。

**处置**：本轮重写报告，覆盖让号、白名单、门禁入仓、契约 v0.2 全部动作。

### R2 · 6 处仍用旧编号指代改名后的裁决（铁律 11）

**分两类处置，不是一刀切**：

- **`report.json` 里的 3 处** —— 那是**当下交接**文档，必须正确 → 本轮重写时改掉。
- **worklog 里的 3 处** —— 那是**历史叙事**，写的时候它确实叫 J6。
  **不改写正文**（改写留痕等于篡改），改为在文首加一行编号注记：
  「本文写作时该裁决编号为 J6，后因撞号让号为 J7，读到 J6 请按 J7 理解」。

区分的依据是文档的时间维度：交接面要「现在是对的」，叙事面要「当时是真的」。

### R3 · §4.1 是假警报，比拖着没清更糟

reviewer 说得对，我逐行核实了：清单点名的三处（监督矩阵、CFO 角色卡、
consulter 审查提示词）**在我本轮 rebase 进来的那批里已全部落地**。
我编辑了 §4.1 那一格，手指按在上面却没打开文件看。

**假警报的危害方向是反的**：拖着没清是「该做的没做」，
假警报是**「清单指着已经正确的文件」** —— 下一个 agent 照它改，会把对的改回错的。

**处置**：撤下三条假警报，挂上真残留 —— `supervision.md:33`（J6 职位状态节内）
仍写「consulter 改的框架仍由 CFO 审」，**与同一文件 :21 的监督矩阵自相矛盾**。
矩阵改对了，节内文字没跟上。§4 台账里 J7 的状态列同步更正为「部分落地」。

## reviewer 审出的另外两条实体问题（本轮已修）

- **A-01 门禁没入仓**：四模块 checks 17 文件 + mission_complete 已全部入仓并复验红测。
- **A-02 根仓停线旗失效**：根仓 `.git/hooks/pre-commit` 还是旧版 306 字节。
  已重装（1445 字节）并红测：置旗 → 提交被拦、HEAD 未动、正确打印停线原因。
  **它卡里那个紧急闸，此前在它自己的仓里是坏的。**

## 自决记账：cockpit-mongo 容器

按「叫人判据」，绑 `127.0.0.1` 的本机开发基建属 CFO 自决区。已执行：

```
容器 cockpit-mongo · 镜像 mongo:7 · 绑定 127.0.0.1:27117 · 卷 cockpit-mongo-data
```

避开 27017（机器上另两个 mongod 属别的系统，碰它们违反铁律 8）、名字带项目前缀、
`docker rm -f cockpit-mongo` 一条命令可回收。绑定面已验：`27017/tcp -> 127.0.0.1:27117`，
宿主只在回环口监听，**未扩大暴露面**。

## 依赖漂移说明

无包依赖增删。契约变更（nexus-core v0.2）的影响面已在该模块 worklog 逐条写明：
纯增量，三个消费方零感知。
