# 任务单 · 开源数据护栏 —— 清 Playwright 残留 + gitignore（CFO 自领）

- 角色：cfo · 日期：2026-08-08 · tier：simple
- 授权：用户裁决「项目要开源，代码可公开，真实时间安排数据不能传上去」。

## 目标

开源前置数据安全：把误入框架仓的真实计时数据残留清出当前树，并加护栏防再渗。

## 背景（扫描实情）

- 数据漏点**单一**：框架仓 `agents/cfo/.playwright-mcp/`，25 个我历史会话的 Playwright
  调试残留（page-*.yml / *.png / console-*.log），其中 page 快照含真实计时明细
  「01:24–01:24 · 英语学习 / 英语单词学习」。纯调试垃圾，本不该 tracked。
- 六模块代码仓命中的「英语学习」等是测试 fixture（合成结构，非隐私明细），保留。
- nginx-docker 的 favicon-32.png 是产品资产，保留。
- 真实全量数据在 mongo 库 + `cockpit-archived` 备份仓 —— 那两个绝不进代码 monorepo。
- cockpit2026：用户裁定占位密钥，留着，历史不动（与真实数据不同，非隐私）。

## 可触碰目录

`agents/cfo/`（删自己写区的调试残留）、`.gitignore`（根，加数据护栏）。

## 验收标准与自检门

- [x] `.playwright-mcp/` 25 个 tracked 残留从当前树移除（git rm）
- [x] .gitignore 挡 `**/.playwright-mcp/`、`*.archive`、`cockpit-export-*.json`、`dump/`
- [x] `git ls-files | grep playwright` 归零（自检）
- [ ] **历史遗留**：这些残留也在历史里 —— 开源前需 filter-repo 从全历史抹掉
      `.playwright-mcp/`（单目录，干净）。本单只清当前树 + 护栏，历史清理属开源那一步。
