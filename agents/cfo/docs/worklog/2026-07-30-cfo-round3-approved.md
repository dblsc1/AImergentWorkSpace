# worklog · reviewer 第 3 轮 approved + 豁免名单去重

- 日期：2026-07-30
- 角色：CFO
- tier：`simple`

## 目标

落 reviewer 第 3 轮 verdict，并修它顺带记的 A-22。

## 可触碰目录（写边界）

`agents/` `scripts/gates/doc-path-exempt.txt`

## 自检门

1. `./scripts/mission_complete.sh`
2. `grep -c 'logs/STOPLINE' scripts/gates/doc-path-exempt.txt` = 1

## 验收标准

| # | 判据 | 确定命令 |
|---|---|---|
| P1 | verdict 已 approved | `reviewer_opinion.verdict` = `approved`，round=3 |
| P2 | 名单无重复条目 | 去重脚本报「去掉 1 条」，复查计数为 1 |

## reviewer 认了什么

它明说：「**我认的不是「这次写全了」，是你换了取值来源**」——
从暂存区算而不是凭记忆枚举，**把三轮都失效的那个环节（人的记忆）从回路里摘掉了**。

它把这记为「『只有闸门不会忘』的第一次自我应用」。这句评价值得留着：
判一个修复是否算数，看的不是这次对不对，是**下次还靠不靠人记得**。

## 它判我的登记方式：按机制登记，不是变形绕过

它用同一把尺逐条量：路径没变形、没去引号、没藏信息、**没掩盖缺口**
（反而在豁免条目里写明根因并点名 A-12）、用的是检查报错文案自己指定的落点。

并且它**单独核了「没有改你的报告」这一点**：committed 版本 232 行，
6 处跨仓引用原样保留，verdict 与「拦路项：无」完整。

## 它指出两条副作用（我认，一条本轮修）

**① 落点偏大（不修，等 A-12）**：三条路径进的是**框架全局**名单，
于是它们对框架仓里**任何**文档都永久免检了。
将来某份框架文档真写出指向 `code/backend/requirements.txt` 的死链，**不会再报**。

这不是登记方式错，是**手上只有这一把工具** —— 精确的做法需要按目录限定豁免范围，
那是 A-12 的落地内容，归框架侧。已请它写进清单：**A-12 精确落地时应连带撤掉这三条。**

**② `logs/STOPLINE` 重复登记（本轮修）**：它在「运行时生成物」段里本来就有。
功能无害（逐行精确匹配照样命中），但说明我加之前没查重。已去重。

它建议给该名单加一条查重/查过期自检 —— 同意，归框架侧，
理由还是那句：靠人记得查重就等于没查。

## 依赖漂移说明

无。只落 verdict 与名单去重。
