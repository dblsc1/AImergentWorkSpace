# 任务单 · timer-ring/v1 补「计时控制按钮 + 暂停（纯前端）」

派活人：人类 → CFO
日期：2026-09-12

## 目标

把人类 2026-09-12 的三颗按钮与「暂停纯前端、后端接口不变」写进跨模块规范，
让 table 蜂巢中心格与 ring 计时台照同一份实现（共享本机键 `nexus.timer.paused.v1`）。

## 可判定的验收标准

| # | 判据 |
|---|---|
| 1 | 规范列出完成 / 暂停 / 继续 / 取消各自调用的**既有**接口，无新增后端接口 |
| 2 | 写明暂停必须真 stop 的理由 |
| 3 | 写明共享键名、值形状、写入与清除时机、升版规则 |
| 4 | 「已知实现」表登记 table 与 ring 两处实现 |
| 5 | 裁决 worklog 记录人类原话与代价 |

## 可触碰目录（写区）

- `contracts/`
- `agents/cfo/docs/`

## 自检门

- 两个模块仓各自门禁 🟢、测试全绿（table 66 passed；ring 58 passed）
- 规范里提到的文件路径都真实存在
