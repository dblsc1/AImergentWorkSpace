# 2026-08-02 · CFO · 铁律 2 补上机械执行者

## 做了什么

`scripts/checks/_common/18-secret-literal.sh` + `selftest.sh` #61。

此前铁律 2 的唯一相关 gate 是 gitleaks，而它**三重失效**：
配置是空壳零规则、被 `RUN_GITLEAKS_LOCAL=1` 挡着、本机没装二进制。
写了一个多月，一次都没执行过。

## 判据形状

认「`*_PASSWORD|_TOKEN|_SECRET|_APIKEY` 或 JSON `"password"` 赋成**字面量**」，
放行 `$VAR` / `${VAR}` / `os.environ` / `getenv` / 占位符 / 纯散文提及。

**判据里不含任何真实凭据。** 把口令写进判据本身等于为了防泄露而泄露，
而且那样只拦得住这一个，下一个不同的照样漏。

## 断言与反向验证

- 正向：三种形状（shell export / JSON / 裸 token）各被点名；七种合法写法零误伤
- 反向：拿掉判据文件 → #61 变红「铁律 2 零执行者」
- `selftest.sh` 61 PASS / 0 FAIL

## 写它的时候 #51 逮住了我

第一版我在帮助文本里印了「确属误报：`AIMERGENT_MISSION_OVERRIDE=…`」，
**#51 当场变红**：「有逃生口没接问责账本」。

那行本来就是冗余 —— 统一逃生口由 `mission_complete.sh` 提供并记账，每条 check 都适用，
在单条判据里再印一遍没有意义。删掉即绿。

顺带记一个判据弱点：**#51 分不出「提到逃生口」和「实现逃生口」**，
它匹配的是字符串。这次结论恰好正确（那行确实不该有），
但它是「判据只看形状不看后果」的又一例，交 consulter 一并看。

## 谁审

**consulter 审，不是我自审。** 它明确指出「写它的人不该是唯一审它的人」（J7 同一条原则）。
我写它审是干净分工。
