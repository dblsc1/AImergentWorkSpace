# 2026-08-03 · CFO 自派 · 开工考试在教错的报告落点

## 目标
考试答案键停在 J3 之前的旧布局，一夜之间两个 reviewer 照着它教的把 canonical report
放错了地方。修到类：让考试从 `report-schema.md` 读，不自带第二份路径表。
## 验收标准
六个角色的落点逐个与 schema 一致；锚点缺失时响亮失败而不是猜一个。
## 可触碰目录
`scripts/exam.sh`、`scripts/selftest.d/`、`agents/protocol/report-schema.md`、`agents/cfo/docs/`、`logs/`
## 自检门
`selftest.sh` 零 FAIL + `mission_complete.sh` 全绿。
