# 任务单 · 驾驶舱前端设计稿 v1（CFO 自领）

- 角色：cfo（arbiter 自己的文档工作，不碰任何模块 code/）
- 日期：2026-08-07
- tier：simple

## 背景

用户令：全面优化四页前端（login/ring/table/gantt + 共享顶栏），先 grill 清需求再动手。
已问清并拍板：

1. 设备：手机 + 平板 + 桌面，且**多窗并排**（一窗 ring、一窗 table……）→ 窄容器一等公民
2. 视觉：**亮暗双主题**。现状暗底暗字被用户定性为「灾难」→ 对比度要机械校验
3. 深度：视觉统一 + 跨页联动 + 布局重排 + 顺带补交互（ring 取消按钮等）
4. 共享样式架构：**方案 C** —— nginx 注入 `/__cockpit/tokens.css`（唯一事实源随契约），
   各前端仓保留 `:root` 兜底块 + diff 契约表的机械校验；login 只用兜底不注入

## 目标与产出（本单范围 = 设计稿，不含实现）

- `agents/cfo/docs/design/cockpit-v1/`：index.html（四页样机 + 行为规格 + 落地清单）、
  tokens.css（**未来 /__cockpit/tokens.css 的种子**，对比度实测值随文件走）、mock.css、mock.js
- 已 Playwright 亮暗双主题全页截图自检（截图在会话 scratchpad，不入仓——设计稿本身可交互复现）

## 可触碰目录

只写 `agents/cfo/docs/`（设计稿 + 本任务单）。不碰任何模块 `code/`、`review/`。

## 验收标准与自检门

- [x] 四个文件各 ≤500 行（C1；自检门：`wc -l` 已跑，243/115/29/348）
- [x] 亮暗两主题正文对比度 ≥4.5:1（实测值写在 tokens.css 头注释）
- [x] Playwright 双主题全页截图自检通过（唯一 console 报错是设计稿自身无 favicon，无害）
- [x] 用户可打开单文件版自行切主题/切容器宽度

## 后续（不在本单）

用户过目拍板后：开 `agents/contracts/design-tokens-v1.md` 契约 + nginx-docker contract v0.6，
再按设计稿 §6 落地清单派五个模块的活。
