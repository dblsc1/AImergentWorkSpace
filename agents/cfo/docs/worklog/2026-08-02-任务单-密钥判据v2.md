# 2026-08-02 · CFO 自派 · 密钥判据 v2（被 consulter 判 rejected 后重写）

## 目标

v1 实测 14 种明文凭据形状只拦住 2 种，且两条过滤器是**可主动利用的绕过**而非单纯的漏。
重写：判断对象从整行改成右值。

## 验收标准

- 十种坏形状全被点名（含 v1 全漏的：小写裸键、连接串、JSON 引号键、无下划线前缀的 PGPASSWORD/AWS_）
- 十三种合法写法零误伤（含三类真实误报：`CL_PASS=0`、JS `e.pass ===`、续行反斜杠）
- 公共件缺失响亮死，不静默退 0
- 全仓 259 文件实跑，误报可控

## 可触碰目录

`scripts/checks/_common/18-secret-literal.sh`、`scripts/selftest.d/`、`agents/cfo/docs/`、`logs/`

## 自检门

`selftest.sh` 零 FAIL + `mission_complete.sh` 全绿。
