# 2026-08-02 · CFO 自派 · 铁律 2 的机械执行者

## 目标

铁律 2「密钥永不进 Git」写了一个多月，执行者一直是零。补一条判据。

## 验收标准

- 三种形状的明文凭据各被点名（shell export / JSON / 裸 token）
- 七种合法写法零误伤（散文提及 / `$VAR` / `${VAR}` / 占位符 / changeme / os.environ / getenv）
- 判据里**不含任何真实凭据**
- `selftest.sh` 有对应断言，且拿掉判据文件会变红

## 可触碰目录

`scripts/checks/_common/18-secret-literal.sh`、`scripts/selftest.sh`、`agents/cfo/docs/`、`logs/`

## 自检门

`scripts/mission_complete.sh` 全绿 + `selftest.sh` 零 FAIL。
