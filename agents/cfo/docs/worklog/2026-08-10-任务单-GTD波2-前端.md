# 任务单 · GTD 波2-B · 前端 GTD 视图（table 模块，Sonnet）

- 派活方：cfo · 日期：2026-08-10 · tier：normal · 档位：**Sonnet**（D17 普通任务）
- 需求源：`agents/cfo/docs/design/gtd-v1/PRD.md` §1 F-INBOX-2..4、§2 F-TODO-1/3/4/5、§4 F-ACTOR-3、§6 F-REVIEW-2
- 依赖：**仅 nexus v1.5 读/写端**（全 live），不依赖 ai-planner，可即刻全推
- 主题：亮暗双色，走 `contracts/design-tokens-v1.md`，日期用服务端归日（别 `new Date` 拼，F-TODO-2）

## CFO 定的 O1

GTD 视图**折进 table 模块**做 tab/子页（收件箱 / 待办 / 回顾），**不新增 nginx 挂载、不开新模块**，当波即可交付。

## 目标（分条，全部 nexus-only）

1. **F-INBOX-2** 全局快速捕捉：顶栏「＋记一笔」/快捷键 → 只填标题建 task 到 `p_inbox`，不要求选项目
2. **F-INBOX-4/3** 收件箱视图：列 `p_inbox` 下未搬走任务，逐条「归类到…」（PATCH projectId 搬移）/删除
3. **F-TODO-1 显示** 待办区：消费 `GET /api/core/views/next-actions`，可做/等待/过期分组（按 zone），后端已排好序直接渲染
4. **F-TODO-3** 每行播放钮 → 计时页 `?task=<id>`；完成打勾 → PATCH done（下游读端重算，刷新）
5. **F-TODO-5** 待办区落 table 一个 tab
6. **F-REVIEW-2** 回顾视图：消费 `GET /api/core/views/review`，一屏清单，每项可跳对应对象
7. **F-ACTOR-3** AI 写过的对象（`lastWriter="ai"`）显 🤖 小角标（字段已在 v1.5，读得到）
8. **F-TODO-4 留桩** 待办区顶「🤖 让 AI 帮我规划」按钮 = **占位桩**，ai-planner 契约落地后另单接线

## 验收（PRD §9）

- [ ] A1：一键捕捉落 p_inbox，不需选项目（E2E）
- [ ] A2：可做/等待/过期分类 + 权重排序正确渲染（E2E，后端已算，前端只需忠实展示）
- [ ] A3 表现侧：完成任务后下游从等待升可做（PATCH done 后刷新可见）
- [ ] 亮暗双主题两套都不塌（尤其别再暗底暗字）
- [ ] 真浏览器验（min= 之类使 JS 校验够不到的死代码要在真浏览器暴露）

## 硬约束

- 留痕四件套 + report.json（canonical path）+ commit trailer `Agent-Attribution: programmer@table+gtd-w2-frontend`
- 新增交互配 E2E；缺陷修到类 + 留断言（铁律 23）
- 开工先读级联 + table 角色卡
