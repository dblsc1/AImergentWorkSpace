# worklog · 销 P0 障签 —— ai-planner 已上远端

- 角色：cfo · 日期：2026-08-11 · tier：simple

## 目标

`blockers/2026-08-11-ai-planner-无远端.md` 的解除条件已满足，按标志销障，并同步依赖索引。

## 解除依据（不是"命令没报错"）

障签写的解除标志是：`ls-remote` 真看到 SHA、且与本地 `feat/init` 一致，
之后由 CFO 改依赖索引签字。实测：

```
本地 = 84827597693f
远端 = 84827597693f
✅ 一致 —— 东西真在远端了
```

仓：`dblsc1/cockpit-ai-planner`（private，2026-08-11 建）。

## 为什么标志当初写错过

初版标志是 `origin` @ `code/ai-planner/.git/config` —— **假绿**：`git remote add` 纯本地不联网，
配完障签立刻自动解除，而数据仍只在本机。已在 `4cf1822` 改成现在这条
（判据从「本地状态」挪到「CFO 看过远端后签的字」）。**今天这一销，销的是修正后的标志。**

## 顺带扫掉索引里另外两处过时（铁律 11：改一处扫全部）

1. 「ai-planner 现在正处于这个状态（无远端）」—— 已改为过去式，并注明它
   **尚未做过「闸门已实证」的三条反向验证**，故不给 ✅（配了远端 ≠ 验过闸门）。
2. gantt `report.json` 的 `status="submitted"` —— 已修（`6ee39902`）。
   **顺便记一笔：这条被波2 审计判为 P2「局部过时」，实际直接卡住了该模块推送
   （`run-gates.sh` exit 1）——审计的严重度判轻了。**

## 本轮推送战果（一并记，SHA 全部 `ls-remote` 核过）

七个仓 + 三条框架修复分支全部落地：
nexus-core `3d7fd605` / table `6d0abc51` / ring `0bbcf45f` / nginx-docker `55e5a670` /
auth `eaa7a742` / gantt `6ee39902` / 根仓 `a0f818de` / ai-planner `84827597`；
框架分支 `d9159c96`、`1e2d979d`、`ba02eeb1`。

**`origin/v5` 仍是 `0d179881` 未动** —— 三条框架修复只进各自分支，J7 评审门完好。

**门禁挡下四个模块**，全是 canonical report 元数据不合规（`git.base` 填错 ×3、
`status` 枚举 ×1、`process_issues` 形状 ×1、超行数未登记 ×1），**零业务代码问题**。
修的过程还有净收益：`data.test.js` 523→按行为拆两份（75 passed 零损失）、
`views_predicates.py` 501→398。
**用户授权的是 `PUSH_UNREVIEWED`（跳过人工审核轮次），我全程没碰 `SKIP_GATES`**
——后者是把没跑绿的东西推上去，性质不同。

## 可触碰目录

`agents/cfo/docs/`、`blockers/`。

## 验收标准与自检门

- [x] 解除标志按原文核验通过（远端 SHA 与本地一致）
- [x] 依赖索引该行更新为真实远端名
- [x] 索引里另外两处随之过时的描述一并扫掉（铁律 11）
- [x] 障签文件删除

## 补记：逃生口记账入仓（门禁当场拦下）

提交时 `checks/08 ledger-tracked` 红：**我这轮用掉的三条 `PUSH_UNREVIEWED` 记账还没入仓**。
门禁原话：「未提交的证据不是证据」。

三条分别是根仓 `feat/gtd-w2-cfo-prep`、以及两条框架修复分支各一次
（模块仓的记账各自留在各自仓里，随各自提交已入仓）。

**这正是逃生口的设计意图**：绕过一道门是被允许的，代价是**必然留下一条谁也删不掉的记录**。
逃生口的全部合法性就是这句话；记录不入仓，那句话就是假的。

**顺带**：今晚有两个 agent 撞上过同一个形状——我推送失败时留下的孤儿记账，
让它们连自己的提交都做不了，只能替我把记录先提交掉。两个都如实上报了。
table 那份 report.json 里 programmer 早把它记成结构性问题
（`issue_id: checks16-ledger-override-deadlock`，`status: open`）：
**推失败 → 留孤儿记账 → 下一个人被卡**。这条仍开着，等裁决。
