# 2026-08-03 · CFO 自派 · 密钥判据误报修到类

## 目标

一小时内两个独立 agent 各自撞上同一条判据的误报，都被迫用了记账逃生口。
误报到逼人用逃生口的程度，判据就等于没有。修到类，不是逐条加白名单。

## 验收标准

- 两个 agent 撞的三条写法全部零误报
- 三种真阳性（shell 赋值 / JSON / 弱口令）仍全部抓得住
- `selftest` #61 不回归

## 可触碰目录

`scripts/checks/_common/18-secret-literal.sh`、`agents/cfo/docs/`、`logs/`

## 自检门

`selftest.sh` 零 FAIL + `mission_complete.sh` 全绿。
