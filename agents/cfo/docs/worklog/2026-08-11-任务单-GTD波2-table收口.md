# 任务单 · GTD 波2-D · table 收口（契约登记 + 渲染分支真数据验证）

- 派活方：cfo · 日期：2026-08-11 · tier：normal · 档位：**Sonnet**
- 上游：波2-B 前端交付（commit `d1b9dab`，已 Git 核验，10 单测绿 + 反向验证过）留下的两个缺口

## 目标

补齐波2-B programmer **因写边界够不着**而上报的两件事：契约登记（arbiter 活）+
四个**只做了代码审查、没被真数据触发过**的渲染分支的实证。

## 缺口一：契约登记（arbiter 帽）

波2-B 新增了三处对外消费，但 `code/table/module_docs/contract.md` 的 `consumes` 没登记：
- `nexus-core.views.next-actions.v1`（待办区）
- `nexus-core.views.review.v1`（回顾）
- `nexus-core.planner.crud.v1` 的**列表读端**（`GET /api/core/planner/{projects,tasks}`，用于 🤖 角标的 `lastWriter` 映射）

programmer 只读契约、checks/81 硬拦，它没越权是对的。**本单由 arbiter 补登记**，铁律 8：模块只经契约依赖，
依赖不登记＝隐性耦合。

## 缺口二：四个渲染分支没被真数据验证过

波2-B 报告明说：**过期 / 今日到期 / 依赖环警示 / 等待-被谁 block** 四条渲染分支
「schema 级代码审查」通过，但**库里没数据触发**，没在真浏览器见过。这四条恰是待办区最容易写错的部分
（A2 验收的核心）。本单造真数据把它们逐条点亮。

**造数据纪律（硬要求）**：
- 生产库有用户真实项目（英语学习/读书计划/X-structure/上班）——**只准新建自己的测试对象，
  绝不修改/删除任何既有对象**
- 测试对象用可识别前缀命名，**用完必须删干净**，并核对库状态回到验证前
- 验证前后各导一次 `GET /api/core/export` 的对象计数做比对，写进报告

## 验收标准

- [ ] contract.md `consumes` 三条登记齐，与实际调用一致；`contract-changelog.md` 记一条
- [ ] 过期（计划期 end < 今天且未完成）→ 琥珀标真出现（截图/DOM 断言）
- [ ] 今日到期 → 置顶排序真生效
- [ ] 依赖环 → 警示标出现且**页面不卡死**（后端已有环防御，前端要能显示）
- [ ] 等待项 → 「被谁 block」显示的是正确的前置任务名
- [ ] 亮暗双主题下四条分支都不塌
- [ ] 测试数据清理干净，前后对象计数一致（写进报告）

## 可触碰目录（写边界）

`code/table/`（模块自有仓）。nexus 契约只读——发现 nexus 侧问题**停下报 CFO**，别自己改 nexus。

## 自检门

- `code/frontend/run_tests.sh` 全绿（现 10 passed 基线只许涨）
- 着陆检查全绿；`gates-fresh` 若红见下「已知红」
- 真浏览器验（不是只看代码）

## 已知红（CFO 已授权 override，别为它改代码）

`gates-fresh` 会红：框架根 `scripts/checks/_common/18-secret-literal.sh` 被 consulter 修过
（`d9159c9`，修 `cfg.x`/`self.x` 误报），**7 个模块镜像全落后**，与本轮改动无关。
镜像同步不在你写区，CFO 统一扫。撞到就 `AIMERGENT_MISSION_OVERRIDE` 放行并写清该理由。
**禁止为了过门去改代码迎合扫描器。**

## 硬约束

- 铁律 8 契约登记；铁律 17 新增代码同批测试；铁律 23 缺陷修到类 + 留断言
- 留痕四件套 + report.json 入仓 commit；trailer 恰一条 `Agent-Attribution: <role>@table+gtd-w2-table-closeout`
- 开工先读级联：根 AGENTS → agents/AGENTS → CONSTITUTION → 模块 AGENTS → 角色卡
