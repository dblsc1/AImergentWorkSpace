# 任务单 · 派单档位裁决入宪（CFO 自领）

- 角色：cfo · 日期：2026-08-09 · tier：simple

## 目标

用户裁决子代理派单档位，固化为宪法裁决（口头裁决必须落台账，否则下一个 agent 读不到）。
用户原话「写入 claude.md，普通任务 sonnet，复杂任务 opus，禁止 fable subagent」。
按铁律 10，项目根 CLAUDE.md 只能是 `@agents/AGENTS.md` 指针，故落 CONSTITUTION §4 裁决台账（级联必读）。

## 裁决内容（D17）

- 普通任务派 **Sonnet**
- 复杂任务（判断/审核/裁决/契约设计/事故定性）派 **Opus**
- **禁止 fable 作 subagent**
- CFO 每次派 subagent 显式传 model，按「有没有判断」分档，不按活大小

## 可触碰目录

`agents/CONSTITUTION.md`（加 D17）、`agents/cfo/docs`（本任务单）。memory 在仓外单独更新。

## 验收标准与自检门

- [x] CONSTITUTION §4 台账新增 D17，格式与既有行一致（编号/日期/内容/提出者/状态）
- [x] 项目根 CLAUDE.md 保持 `@agents/AGENTS.md` 不变（铁律 10）
- [x] memory feedback_delegate 同步（反映禁 fable + opus 复杂档）
- [x] mission_complete 全绿
