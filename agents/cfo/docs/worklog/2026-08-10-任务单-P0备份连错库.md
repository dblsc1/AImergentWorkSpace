# 任务单 · P0 生产备份连错库（CFO 自领）

- 角色：cfo · 日期：2026-08-10 · tier：simple · **P0 数据安全**

## 目标

修 compose 迁移埋的雷：生产 mongo 没发布宿主端口，宿主 backup.sh 连到空的测试容器，
**生产数据从没被备份**。

## 背景（诊断实据）

- 生产数据在 `cockpit-c-mongo`（compose 内网 27017，volume `cockpit-mongo-data`）——
  英语学习/读书计划/X-structure/上班 四项目在这。
- compose mongo **未发布宿主端口**（切换 compose 时 CFO 漏了宿主工具依赖 127.0.0.1:27117）。
- 宿主 27117 被 nexus agent 波 1 测试遗留的 `nexus-core-testmongo`（events=0 空库）占着。
- `scripts/backup.sh` 连 127.0.0.1:27117 → 备份空 testmongo → **生产没被备份**。

## 可触碰目录

`deploy/`（compose 加端口）、`agents/cfo/docs`。

## 验收标准与自检门

- [x] compose mongo 发布 `127.0.0.1:27117:27017`（绑回环，不 LAN 暴露 mongo）
- [x] 清掉 agent 遗留的 nexus-core-testmongo（占端口误导）
- [x] recreate mongo（volume external 保护，数据不丢）
- [x] backup.sh 连 27117 实测能看到生产四项目（不是空库）
- [x] 铁律 23：加断言/文档防复发（compose 端口 vs 宿主工具依赖的类）
