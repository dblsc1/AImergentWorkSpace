# 架构问题清单（cfo_reviewer 实时维护）

> 一行一问题，当场登记、当场按 `agents/protocol/supervision.md` 阻塞判据分流。
> 阻塞 → 置 `logs/STOPLINE` + 上报；高危不阻塞 → 限期标注；不阻塞 → 攒批。
> 本清单是 consulter 模式监督的主要输入。

| 日期 | 现象 | 类别 | 判定 | 去向 |
|---|---|---|---|---|
| 2026-07-30 | **A-01** 四模块（ring/table/nexus-core/nginx-docker）的门禁装载全部只在工作区：`scripts/checks/`、`scripts/hooks/pre-commit`、`scripts/mission_complete.sh` 在各仓 HEAD 上都不存在。任一 `clone`/`checkout .`/`stash` 即回到「pre-commit 用 `[ -x … ] \|\| exit 0` 静默跳过」的事故态。`checks/13` rc=0 是工作区绿，比对的是未入仓副本 | 流程 / 门禁 | **高危不阻塞**（恒绿：门失效但不卡人） | 限期：下一批清账**必含**。四模块各 commit 门禁装载 + 红测脚本入 `review/reviewcode/`（铁律 19）。已写进本轮 verdict 关闭条件 |
| 2026-07-30 | **A-02** 框架根仓自己的 `.git/hooks/pre-commit` 是旧版（306B，`[ -x … ] \|\| exit 0`），与仓内 `scripts/hooks/pre-commit`（新版，含 STOPLINE 冻结 + 缺文件 die）不一致；`pre-push` 亦不一致。**后果：`logs/STOPLINE` 停线旗在本仓不生效**——cfo_reviewer 卡里写的紧急闸在它自己的仓里是断的 | 流程 / 门禁 | **高危不阻塞**（恒绿；但它使停线机制在根仓失效） | 限期：本仓重跑 `scripts/install-gates.sh .` 并实测停线旗红/绿。铁律 20（根仓自身也受治理）。转 CFO 执行、consulter 知会 |
| 2026-07-30 | **A-03** 宪法 §4.1 待改清单三项（supervision.md 矩阵行 / cfo/AGENTS.md / consulter 审查提示词）**在 HEAD 上已全部落地**（`fc70c87`，是 HEAD 祖先），但 §4.1 与 §4 的 J7 状态列仍写「未落地」。§4.1 是「防止 agent 照旧契约实现出与裁决相反的东西」的那道防线，现在指向错误方向——下一个 agent 可能按过期清单把已正确的文件改回去 | 文档 / 裁决台账 | **高危不阻塞**（有合法绕行：读者可自行核文件；但反向改回的风险真实） | 限期：本轮复审必须关闭（打回项 R3）。属 CFO 写区，CFO 自清 |
| 2026-07-30 | **A-04** `agents/protocol/supervision.md` 自相矛盾：第 21 行矩阵写「consulter 框架改动 → **人类**审（J7）」，第 33 行（J6 节）仍写「consulter 改的框架**仍由 CFO 审**」。该文件自称「谁审谁的唯一事实源」，两处口径并存。**注：CFO 本轮上报的是第 21 行有问题——报错了对象，真残留是第 33 行** | 文档 / 流程 | **不阻塞**（攒批） | 框架区，按 J7 归人类审 / consulter 改。CFO 不代改是对的。攒批交 consulter 模式监督 |
| 2026-07-30 | **A-05** 裁决编号 `J6→J7` 让号后，全仓 6 处仍用 `J6` 指代改名后的裁决（`agents/cfo/docs/report.json` 3 处、`…worklog/2026-07-29-cfo-review-framework-6cceec1-4bf3dfd.md` 3 处）。「一个号指两件事」的问题没消灭，只是从台账移到了留痕里 | 文档（铁律 11） | **不阻塞**（攒批） | 本轮打回项 R2，CFO 自清。**模式提示**：分布式编号无协调已是第三次（selftest 断言撞 35、同日两条 J6、本条）——是否需要编号申领机制交 consulter |
| 2026-07-30 | **A-06** 宪法合并（`b190063`，依人类裁决 D13）去掉了 v5 侧文件头的「**级联铁律**：本文件只能加严，**不得放松或改写**上层任一条」以及 §1 的「加严」定性与「加严的是哪条通用铁律」一列。本文件现自称「唯一宪法」，而**方向性约束（只能加严）在本文件里已无载体**（`agents/AGENTS.md` 仍有同义表述，非真空） | 架构 / 文档 | **不阻塞**（攒批） | 有裁决背书（D13），非违规。建议人类决定是否在 §1 补回一句。攒批 |
| 2026-07-30 | **A-07** `agents/AGENTS.md` 铁律 20 写「clone 后第一件事是 `./ci/install-ci.sh .`」，但仓内**不存在 `ci/` 目录**；实际安装脚本是 `scripts/install-gates.sh`。照条文做的人会撞空路径，然后大概率跳过装门禁 | 文档 / 流程 | **不阻塞**（摩擦性；有合法绕行——`scripts/install-gates.sh` 可用） | 框架区，归 consulter。攒批。与 A-02 同源（根仓门禁没人装的其中一个原因） |
| 2026-07-30 | **A-08** `code/nginx-docker` 把生成物纳入 Git 跟踪（`logs/INDEX.md`、`logs/console.json`、`logs/diary.jsonl` 均为 ` M` 跟踪态），与框架 `.gitignore`「生成物由脚本产出不入仓（入仓会让每次提交后工作树永远脏）」相反。该仓工作树因此恒脏，掩盖真实改动 | 流程 / 数据边界（铁律 7） | **不阻塞**（攒批） | 模块级，交 nginx-docker 属主（D5：暂由 CFO 直管）。攒批 |
