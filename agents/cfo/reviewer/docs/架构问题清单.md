# 架构问题清单（cfo_reviewer 实时维护）

> 一行一问题，当场登记、当场按 `agents/protocol/supervision.md` 阻塞判据分流。
> 阻塞 → 置 `logs/STOPLINE` + 上报；高危不阻塞 → 限期标注；不阻塞 → 攒批。
> 本清单是 consulter 模式监督的主要输入。

| 日期 | 现象 | 类别 | 判定 | 去向 |
|---|---|---|---|---|
| 2026-07-30 | **A-01** 四模块（ring/table/nexus-core/nginx-docker）的门禁装载全部只在工作区：`scripts/checks/`、`scripts/hooks/pre-commit`、`scripts/mission_complete.sh` 在各仓 HEAD 上都不存在。任一 `clone`/`checkout .`/`stash` 即回到「pre-commit 用 `[ -x … ] \|\| exit 0` 静默跳过」的事故态。`checks/13` rc=0 是工作区绿，比对的是未入仓副本 | 流程 / 门禁 | **高危不阻塞**（恒绿：门失效但不卡人） | 限期：下一批清账**必含**。四模块各 commit 门禁装载 + 红测脚本入 `review/reviewcode/`（铁律 19）。已写进本轮 verdict 关闭条件 　**【R2 复核：部分已修】** ring `cd4d1c0` / table `061c745` / nexus-core `fff59d4` 三仓三件套已在 HEAD（各 17 个 checks），`scripts/` 干净；**nginx-docker 未修 → 转 A-09**。红测脚本入仓一项仍未做。 　**【R3 复核：关闭】** 四模块 `scripts/` 全干净、`13-gates-fresh` 我逐仓实跑 4/4 rc=0。**唯一遗留**：红测仍无入仓脚本（`review/reviewcode/` 里查无），转 A-17。 |
| 2026-07-30 | **A-02** 框架根仓自己的 `.git/hooks/pre-commit` 是旧版（306B，`[ -x … ] \|\| exit 0`），与仓内 `scripts/hooks/pre-commit`（新版，含 STOPLINE 冻结 + 缺文件 die）不一致；`pre-push` 亦不一致。**后果：`logs/STOPLINE` 停线旗在本仓不生效**——cfo_reviewer 卡里写的紧急闸在它自己的仓里是断的 | 流程 / 门禁 | **高危不阻塞**（恒绿；但它使停线机制在根仓失效） | 限期：本仓重跑 `scripts/install-gates.sh .` 并实测停线旗红/绿。铁律 20（根仓自身也受治理）。转 CFO 执行、consulter 知会 　**【R2 复核：已修并复验，关闭】** 活钩子与仓内副本逐字节相同，框架根 `13-gates-fresh.sh` rc=0。且 consulter 在 `00a6d4c` 给 13 号加了「活钩子自比 + 拒未入仓副本、框架型仓不豁免」——**修到类而非修到点**。 |
| 2026-07-30 | **A-03** 宪法 §4.1 待改清单三项（supervision.md 矩阵行 / cfo/AGENTS.md / consulter 审查提示词）**在 HEAD 上已全部落地**（`fc70c87`，是 HEAD 祖先），但 §4.1 与 §4 的 J7 状态列仍写「未落地」。§4.1 是「防止 agent 照旧契约实现出与裁决相反的东西」的那道防线，现在指向错误方向——下一个 agent 可能按过期清单把已正确的文件改回去 | 文档 / 裁决台账 | **高危不阻塞**（有合法绕行：读者可自行核文件；但反向改回的风险真实） | 限期：本轮复审必须关闭（打回项 R3）。属 CFO 写区，CFO 自清 　**【R2 复核：已修，关闭】** §4.1 已撤三条假警报、换上真残留（`supervision.md` 第 33 行），并把撤销理由留在原地；§4 台账 J7 状态列改为「部分落地」。 |
| 2026-07-30 | **A-04** `agents/protocol/supervision.md` 自相矛盾：第 21 行矩阵写「consulter 框架改动 → **人类**审（J7）」，第 33 行（J6 节）仍写「consulter 改的框架**仍由 CFO 审**」。该文件自称「谁审谁的唯一事实源」，两处口径并存。**注：CFO 本轮上报的是第 21 行有问题——报错了对象，真残留是第 33 行** | 文档 / 流程 | **不阻塞**（攒批） | 框架区，按 J7 归人类审 / consulter 改。CFO 不代改是对的。攒批交 consulter 模式监督 　**【R2 复核：仍开】** 已由 CFO 挂进宪法 §4.1 待改清单第 ① 项，落地仍待人类/consulter。 |
| 2026-07-30 | **A-05** 裁决编号 `J6→J7` 让号后，全仓 6 处仍用 `J6` 指代改名后的裁决（`agents/cfo/docs/report.json` 3 处、`…worklog/2026-07-29-cfo-review-framework-6cceec1-4bf3dfd.md` 3 处）。「一个号指两件事」的问题没消灭，只是从台账移到了留痕里 | 文档（铁律 11） | **不阻塞**（攒批） | 本轮打回项 R2，CFO 自清。**模式提示**：分布式编号无协调已是第三次（selftest 断言撞 35、同日两条 J6、本条）——是否需要编号申领机制交 consulter 　**【R2 复核：已修，关闭】** 分两类处置：`report.json` 三处随重写清除；worklog 正文不改、文首加编号注记。**该处置应立为判例**——依据成文于 `60-doc-paths-exist.sh` 第 11–13 行（历史叙事不可被强制篡改）与 `AGENTS.md` 文档体系表（worklog=回顾过去 / report=当下交接）。 |
| 2026-07-30 | **A-06** 宪法合并（`b190063`，依人类裁决 D13）去掉了 v5 侧文件头的「**级联铁律**：本文件只能加严，**不得放松或改写**上层任一条」以及 §1 的「加严」定性与「加严的是哪条通用铁律」一列。本文件现自称「唯一宪法」，而**方向性约束（只能加严）在本文件里已无载体**（`agents/AGENTS.md` 仍有同义表述，非真空） | 架构 / 文档 | **不阻塞**（攒批） | 有裁决背书（D13），非违规。建议人类决定是否在 §1 补回一句。攒批 |
| 2026-07-30 | **A-07** `agents/AGENTS.md` 铁律 20 写「clone 后第一件事是 `./ci/install-ci.sh .`」，但仓内**不存在 `ci/` 目录**；实际安装脚本是 `scripts/install-gates.sh`。照条文做的人会撞空路径，然后大概率跳过装门禁 | 文档 / 流程 | **不阻塞**（摩擦性；有合法绕行——`scripts/install-gates.sh` 可用） | 框架区，归 consulter。攒批。与 A-02 同源（根仓门禁没人装的其中一个原因） |
| 2026-07-30 | **A-08** `code/nginx-docker` 把生成物纳入 Git 跟踪（`logs/INDEX.md`、`logs/console.json`、`logs/diary.jsonl` 均为 ` M` 跟踪态），与框架 `.gitignore`「生成物由脚本产出不入仓（入仓会让每次提交后工作树永远脏）」相反。该仓工作树因此恒脏，掩盖真实改动 | 流程 / 数据边界（铁律 7） | **不阻塞**（攒批） | 模块级，交 nginx-docker 属主（D5：暂由 CFO 直管）。攒批 　**【R2 复核：范围扩大，仍开】** 不止 nginx-docker：`ring` / `table` 同样跟踪 `logs/INDEX.md`+`console.json`+`diary.jsonl`，`ring` 还跟踪 `logs/sessions/ring.programmer_reviewer.session`；`nexus-core` 只跟踪 `diary.jsonl`——**四个模块四种口径**。四仓工作树因此恒脏，掩盖真实改动。 |
| 2026-07-30 | **A-09** `code/nginx-docker` 的门禁增量仍未入仓：6 个已跟踪文件（`scripts/hooks/pre-commit` 本体、`checks/_common/12-standing-docs.sh`、`checks/arbiter/80-taskcard-complete.sh`、`gates/doc-path-exempt.txt`、`mission_start.sh`、`arbiter-push.sh`）现行版本只在工作区。比 A-01 轻（HEAD 上有早先提交的三件套，clone 不裸奔）但同形。CFO 口径称「四模块已完成」，实际 3/4 | 流程 / 门禁 | **高危不阻塞**（恒绿：装的是旧版） | 限期：本轮 verdict 关闭条件 N3。与 A-10 同一条命令可一并解决 　**【R3 复核：已修，关闭】** nginx-docker `scripts/` 脏文件数 0。 |
| 2026-07-30 | **A-10** 四模块**此刻全部 `gates-fresh` 红**（`code/ring` 实跑 `mission_complete.sh` rc=1，`nginx-docker` 同）。成因：00:40 装门禁 → 01:09 rebase 到新 v5（`00a6d4c` 改了 13 与 90）→ 之后未重装；sha256 确认模块里装的是 `00be505` 旧版。**这正是 CFO 上一轮亲手写进 worklog 的「必须先 rebase 再装」，一轮后原样复发** —— 说明该教训只活在散文里，无机械保证 | 流程 / 门禁 | **高危不阻塞**（有记账逃生口 `AIMERGENT_MISSION_OVERRIDE`，且一条 `install-gates.sh` 即绿，不构成恒红） | 限期：**派活前置条件**（本轮 N2）。**类修建议**：`install-gates.sh` 应在「框架仓落后于 origin」时拒绝安装，或装完打指纹——框架区，交 consulter 　**【R3 复核：已修，关闭】** 先 `fetch` 确认 `HEAD..origin/v5`＝0 再装，4/4 rc=0（我逐仓实跑）。**类修（install-gates 在框架落后 origin 时拒装）仍未做**，CFO 已记 worklog 归框架侧——保留为 A-18。CFO 本轮的定性值得留作判例：**「教训只活在散文里就没有约束力，只有闸门不会忘」**。 |
| 2026-07-30 | **A-11** `code/nexus-core/codeagent/arbiter/docs/report.json` 自相矛盾：CFO 在 `fff59d4` 改了 `task`/`summary` 描述本轮工作，却留着 `status: "blocked"` 与「两条升级都需 CFO 裁决后才能开单」的 escalation——**而该 commit 本身就是那两条裁决**（起 `cockpit-mongo` = 升级 1 的倾向选项；contract v0.2 补 planner 只读面 = 升级 2 的 A 案）。CFO 下一步即向该模块派活，冷启动 programmer 会读到「blocked、等裁决」 | 文档 / 流程 | **高危不阻塞**（与 §4.1 假警报同形：留痕指着已不成立的状态） | 限期：**派活前置条件**（本轮 N1）。附带：`reviewer_opinion.reviewer` 应为 `module_reviewer`（现填 programmer_reviewer）、`path` 指向的是 arbiter 自己的 worklog 而非 reviewer 详报、`cross_module_impact` 为空但契约新增两个对外能力 　**【R3 复核：主体已修，剩一格】** `cc805a1`：`status`→`approved`、`escalation`→`null`、summary 写明两条裁决的落地物、`reviewer_opinion.reviewer`→`module_reviewer`。**仍未修**：`reviewer_opinion.path` 依旧指向 arbiter 自己的 worklog（应为 reviewer 详报，report-schema 第 35 行）、`cross_module_impact` 仍为 `[]`（契约 v0.2 新增两个对外能力）。因 `verdict` 为 `pending`、真 reviewer 落笔时会覆盖，不阻塞。 |
| 2026-07-30 | **A-12** `60-doc-paths-exist.sh` 把反引号里的 `` `路径:行号` `` 整串当路径解析（正则 `` `(agents\|code\|logs\|scripts)/[^`]+` `` 未剥离结尾 `:<数字>`），判成死链。**三层问题**：① 检查缺陷，剥掉 `:<数字>` 一行即修；② **与 `agents/cfo/reviewer/agent.md` 第 42 行「verdict 逐条 文件:行」直接打架**——框架命令用该格式又用门禁拒绝它，每轮复发且最省事解法是「去反引号」，**正是红线明令禁止的那一步**；③ 历史叙事豁免只覆盖 `*/docs/worklog/*`、`*/docs/findings/*`、`*/docs/decisions/*`，**cfo_reviewer 报告落在 `agents/cfo/reviewer/docs/*.md` 不在名单内**，直接导致被审方去改审计方的报告（本轮 N4 的根因） | 流程 / 门禁 / 角色卡 | **高危不阻塞**（摩擦性、有合法绕行；高危在于它稳定把人推向红线那一步） | 限期：下一批清账必含。框架区，按 J7 交人类审、consulter 改。**本轮 CFO 的改写判为「不是绕过」**：路径仍在反引号内、仍受门禁管辖、行号信息未丢、未掩盖任何缺口——与 2026-07-29「去反引号」判例的关键区别 |
| 2026-07-30 | **A-13** `cockpit-mongo` 自决记账中「`docker rm -f cockpit-mongo` 一条命令可回收」不准确：具名卷 `cockpit-mongo-data` 不随容器删除（已确认该卷存在），完整回收需两条命令。**「一条命令可回收」正是把此事划进 CFO 自决区的判据条件**，故措辞须准确 | 文档 / 记账 | **不阻塞**（攒批） | 记账其余各项复核属实：`ss -tlnp` 仅 `127.0.0.1:27117`、命名带项目前缀、report.json 与 worklog 均已登记并 commit。修措辞即可 　**【R3 复核：已修，关闭】** worklog 已改为 `docker rm -f cockpit-mongo && docker volume rm cockpit-mongo-data`。容器活性我另行实测：`ping ok=1`、仅 `127.0.0.1:27117`。 |
| 2026-07-30 | **A-14** D5 裁决「nginx 与容器化独立成模块 `code/nginx-docker`，任务暂由 CFO 直管」，但新起的 `cockpit-mongo` 容器在该模块内**无任何记录**（全仓 grep 为空），只登记在框架根 CFO 的 report/worklog。拥有容器化归属的模块看不见本项目已存在的容器 | 文档 / 归属（铁律 11） | **不阻塞**（攒批） | 建议在 `code/nginx-docker` 留一行容器台账（名称/镜像/绑定/卷/回收命令），否则该模块接手部署面时会漏 |
| 2026-07-30 | **A-15** 即将派给 `codeagent/programmer/1` 的任务单 `code/nexus-core/module_docs/worklog/2026-07-30-任务单-切片1-金链路.md`（tier `hard`）**处于未提交状态**。冷启动 programmer 读不到工作区，而这张单是它全部的任务定义。与 nexus-core escalation 结尾自己写的「不能拿 staged 状态当判据」是同一句话，只是这次轮到派单方 | 流程 / 留痕（铁律 12/18） | **高危不阻塞**（未派活前无实害；一旦发签即成实害） | 限期：**派活前置条件**，并入本轮 N1 关闭条件 　**【R3 复核：已修，关闭】** 任务单 `module_docs/worklog/2026-07-30-任务单-切片1-金链路.md` 已 `git ls-files` 可见。 |
| 2026-07-30 | **A-16** `agents/cfo/reviewer/session`（1037 B，内容为 `agent_id` 续用句柄，已确认不含任何密钥/凭据）**处于已暂存未提交状态**，且口径不一致：`scripts/gates/doc-path-exempt.txt` 第 25 行把它归入「运行时生成物（.gitignore 忽略，跑起来才有）」，但 `.gitignore` 里**没有**它，CFO 又把它 `git add` 了。三处口径三个样 | 数据边界（铁律 7） | **不阻塞**（攒批） | 二选一并统一：① 真当运行时数据 → 加进 `.gitignore` 且不提交；② 真要入仓 → 从豁免名单的「运行时生成物」分类里挪走。现状是入仓后每次换 session 工作树就脏，正是 `.gitignore` 注释自己警告的那个后果 　**【R3 复核：状态变了，仍开】** `agents/cfo/reviewer/session` 已随 `61b6c92` 入仓（25 行）。于是口径矛盾从「暂存未提交」变成「**已跟踪入仓，却仍被 `doc-path-exempt.txt` 第 25 行归类为「运行时生成物（.gitignore 忽略，跑起来才有）」**」。二选一仍未做。 |
| 2026-07-30 | **A-17** 四模块的门禁红测（裸提交 rc=1 且 HEAD 未动）**至今只有散文，没有入仓脚本**：`code/ring/review/reviewcode/` 下只有 `run_all.sh`，内无任何裸提交/门禁红测逻辑。铁律 19「免掉的审核必须以确定性脚本自核顶上，脚本落 `review/reviewcode/` 并 commit」未满足——门禁是否真拦得住，目前每次都靠人手跑一遍并口述结果 | 流程 / 铁律 19 | **不阻塞**（攒批；门禁本身已入仓且新鲜度绿，红测缺的是可重放性） | 自 A-01 拆出。建议下一批把裸提交红测写成脚本入四模块 `review/reviewcode/` |
| 2026-07-30 | **A-18** `install-gates.sh` 不校验框架仓是否落后 origin，导致「先装后 rebase」拷进旧判据。该坑 CFO 已连踩两轮（2026-07-29 一次、2026-07-30 一次），第二次是在自己已把教训写进 worklog **之后**——实证「散文级教训无约束力」 | 架构 / 门禁 | **高危不阻塞**（每次复发都靠人事后发现；有合法补救＝重跑一条命令） | 限期：下一批清账必含。框架区归 consulter。修法：装前比对 `HEAD..origin/<分支>` 计数，非 0 则拒装；或装完写指纹供 13 号比对 |
| 2026-07-30 | **A-19** CFO 的 `report.json` 交付面自我申报**连续三轮不完整**：R1 整轮无 report → R2 `docs_reviewed` 漏「动过审计方报告」→ R3 `changed_files` 漏本轮 worklog 与两个交付文件、`task` 停在上上轮。形状一致：**正文/summary 写得足，结构化字段落后于正文**。`contains` 语义只校验「声明的都在 diff 里」，漏声明机器抓不到 | 流程 / 留痕（report-schema 第 157 行） | **不阻塞**（不影响派活面与他人；但可审计性逐轮流失） | R3 打回项 M1。**模式提示**：这是同一形状第三次，已超出单点范畴，交 consulter 做模式监督；机械修法＝加一条检查「`changed_files` 必须覆盖本次提交里属于本角色的全部文件」 |
| 2026-07-30 | **A-20** `code/nexus-core` 的常备文档仍是 mock 阶段口径：`code/backend/requirements.txt` 抬头写「本版无持久化层，**不得**加入 pymongo/motor」，`rules.md` 第 18 行「Mongo 真实现才引入」、第 160 行「planner 本轮不建，除非 CFO 裁决走 A 案」——**A 案已裁但未写回 `rules.md`**（该文件自 `c2962f3` 未动）。所幸两处禁令都写成了带条件的形式（「本版没有 repo.py，因此…」「除非…」），条件随切片 1 自解，故不构成冲突 | 文档（铁律 11） | **不阻塞**（条件自解；任务单口径明确） | 建议切片 1 开工第一件事刷新 `rules.md` 与 `requirements.txt` 抬头。**附记**：这两处防御性条件写法预见到了今天的相位切换，值得作为正面判例 |
| 2026-07-30 | **A-21** 派活前置核验（容器可写 / venv 建得起 / pymongo 4.17.0 装得上 / `admin.command('ping')` ok=1.0）只写在任务单散文里，探针落在 `/tmp/claude-1000/probe*`（两个 pymongo wheel 仍在），**仓外、会话结束即蒸发** | 流程 / 铁律 19 | **不阻塞**（任务单已把同五项写成 programmer 的 S 表验收项，下游会重跑，证据强度由下游补回） | 攒批。建议此类前置核验直接落脚本进 `review/reviewcode/` |
