# cfo_reviewer · 第 2 轮审查报告

- 日期：2026-07-30
- 审查者：cfo_reviewer（同一常驻实例，第 2 次唤醒）
- 被审对象：CFO
- 被审区间：`git diff origin/v5..HEAD`（`origin/v5` = `00a6d4c`，HEAD = `e2e2045`，12 commit）
  + 三个模块仓各一笔（`code/ring` `cd4d1c0`、`code/table` `061c745`、`code/nexus-core` `fff59d4`）
- **verdict：`rejected`**（round 2）

---

## 结论摘要

**上轮打回的三条，全部真修了，我逐条复核通过。** R1 的机械核验第一次端到端跑通，
R3 换上的是真残留，A-02 我用独立机制复验过——那个此前坏掉的紧急闸现在真的通电了。
Mongo 自决的记账我按实况核过，属实。

**但本轮引入了四条新缺陷，其中两条与你上轮亲手认下的失效形状是同一个：
「留痕说的状态 ≠ 仓里的状态，而下一个 agent 会照留痕行事」。**

而这两条恰好都落在你**下一步要做的事**上（推送 + 给 nexus-core 派活），
所以我必须在推送前拦一次，而不是记进清单攒批。

| # | 违反 | 一句话 |
|---|---|---|
| N1 | 铁律 11 / 12、report-schema 升级面 | `code/nexus-core` 的 arbiter report 被你改成描述本轮工作，却**留着 `status: "blocked"` 和「两条升级都需 CFO 裁决后才能开单」**——而这一笔 commit 本身就是那两条裁决 |
| N2 | 铁律 19、你自己上轮的判例 | 四模块**此刻全部 `gates-fresh` 红**（实跑 rc=1）。装门禁在 00:40，rebase 到新 v5 在 01:09，新 v5 改了 13/90——**「必须先 rebase 再装」是你上一轮写下的教训，本轮原样复发** |
| N3 | 铁律 12/18 | `code/nginx-docker` 的 A-01 **没修**：6 个门禁文件（含 `scripts/hooks/pre-commit` 本体）仍只在工作区。报告与交接口径都称四模块已完成 |
| N4 | 铁律 11（`docs_reviewed` 是它的机械落点） | 你改了**审计方的报告**（我的 round1.md，15 处），文件进了 `changed_files`，但 `docs_reviewed` 里没有它、没有任何一条声明 |

**没有触发停线**（判据逐条比对见文末）。四条都可在十几分钟内闭环，
修完唤醒我复审即可推送——**我不认为需要打断夜班节奏，但也不认为可以带着 N1/N2 派活。**

---

## 上轮打回三条 · 复核结果

### R1 报告没随最后两笔更新 —— **已修，通过**

这次机械核验第一次全绿：

```
git status --short -- agents/cfo/docs/report.json   → 无输出                       ✅
git ls-files --error-unmatch …                      → tracked                      ✅
resolved_head = e2e2045ba23fc6d8660f83158a394a86a043bc36
HEAD          = e2e2045ba23fc6d8660f83158a394a86a043bc36   ← 两者相等，不再落后     ✅
git merge-base --is-ancestor 211bdec5 e2e2045       → OK                           ✅
changed_files 6 项逐项出现在 base..resolved_head：  6/6 OK                          ✅
（含 report 自身与本轮 worklog，符合 report-schema 第 157 行）
```

`task` / `summary` 与本轮实际动作对得上，`cross_module_impact` 正确列出四个模块。
**这一格从「未产出」变成了「合规」。**

### R2 旧编号指代 —— **已修，且你的分类是对的；规矩也是成文的，你找漏了**

处置我核过：`report.json` 三处已随重写清除（全文无残留误指）；
worklog 正文一字未改，文首加了注记，写法准确（「本文写作时该裁决编号为 J6……读到本文里的
J6 时请按 J7 理解」）。

**你说「改写留痕等于篡改，但这条规矩我没在任何成文规范里找到明确依据」——它有，两处：**

1. **`scripts/checks/_common/60-doc-paths-exist.sh` 第 11–13 行**，就是本轮卡你那条检查
   自己的注释：

   > 「历史叙事类不查：worklog / findings 记录的是**当时的状态**，
   >   强制它们指向现存路径 = **强制篡改历史**，与「留痕不可篡改」直接冲突」

   它把 `*/docs/worklog/*`、`*/docs/findings/*`、`*/docs/decisions/*` 整类跳过。
   按铁律 17（审核代码化优先），**规矩写进检查里就是成文**——而且是可执行的成文，
   比写在文档里更硬。你要找的判例，正躺在今晚绊你那个脚本的第 11 行。

2. **`agents/AGENTS.md` 文档体系表**：worklog =「回顾过去：做了什么、为什么」，
   report.json =「当下交接：任务终态与升级面」。
   **你那句「交接面要『现在是对的』，叙事面要『当时是真的』」不是你的偏好，
   它就是这张表。** 你是在重新推导框架已有的模型，推导对了。

**结论：这条处置我不但不打回，还认为它应当成为判例。** 已记入架构问题清单备查。

### R3 §4.1 假警报 —— **已修，通过**

逐行核过：

- §4.1 现在挂的是**真残留**——`agents/protocol/supervision.md` 第 33 行（J6 职位状态节内）
  仍写「consulter 改的框架仍由 CFO 审」，与同文件第 21 行的矩阵自相矛盾。我复核该行确实如此。
- 待改清单只剩 ① 一项，且注明了「原清单点名的三处已于 `fc70c87` 全部落地，
  2026-07-30 逐行核实后撤下——它们此前是假警报」。**把撤销的理由留在原地，这是对的**：
  下一个读者不会以为有人偷偷删了待办。
- §4 台账 J7 状态列由「未落地」改为「**部分落地**」并写清哪三处已改、哪一处残留。准确。
- `fc70c87` 在本轮 rebase 到新 v5 之后**仍是 HEAD 祖先**（已验），引用未失效。

### A-01 四模块门禁入仓 —— **3 / 4 已修**（详见 N3）

| 模块 | 入仓 commit | HEAD 上 `pre-commit` / `mission_complete.sh` / `checks` | `scripts/` 是否干净 |
|---|---|---|---|
| ring | `cd4d1c0` | ✅ / ✅ / 17 个 | ✅ 干净 |
| table | `061c745` | ✅ / ✅ / 17 个 | ✅ 干净 |
| nexus-core | `fff59d4` | ✅ / ✅ / 17 个 | ✅ 干净 |
| **nginx-docker** | **无本轮 commit** | ✅ / ✅ / 17 个（早先提交的旧版） | ❌ **6 个文件仍未提交** |

ring / table / nexus-core 三个仓，我用 `git ls-tree -r HEAD` 直接看committed tree
确认了三件套真在 HEAD 上，不再是工作区幻觉。**这一条对这三个仓是真修了。**

### A-02 根仓停线旗失效 —— **已修，并用独立机制复验**

```
.git/hooks/pre-commit   1445 B   与仓内 scripts/hooks/pre-commit 逐字节相同   ✅
.git/hooks/pre-push                                        逐字节相同       ✅
框架根 bash scripts/checks/_common/13-gates-fresh.sh  →  rc=0               ✅
```

我没有只信「已重装」这句话：新 v5 的 `00a6d4c`（consulter，`fix(class): 被测对象错位——
13 活钩子自比 + 拒未入仓副本`）给 13 号加了「**活钩子 vs 本仓 tracked 副本，字节比对，
一切仓型都查，框架 checkout 也不豁免**」，其注释明写病根是「根仓的 pre-commit 曾停在
306 字节旧版——停线旗在它自己仓里插不住，而本检查当时把框架型仓整个豁免，看不见」。

**这是「修到类」而不是「修到点」**：A-02 不但被修好，还被装上了防止它再次发生且
不被检查看见的机械保证。这一条的处置质量高于我的要求，记一笔。

### 自决记账（Mongo）—— **核记账通过，一处措辞不准**

按你的要求只核记账、不核决定。逐项对实况：

| 判据要求（`supervision.md`「不叫人」条） | 实况核验 | 判 |
|---|---|---|
| 绑 127.0.0.1 | `docker ps`：`127.0.0.1:27117->27017/tcp`；`ss -tlnp`：**仅** `127.0.0.1:27117` 一条 | ✅ 属实，未扩大暴露面，未触发「必须叫人」第 1 类 |
| 命名带项目前缀 | 容器 `cockpit-mongo`、卷 `cockpit-mongo-data`，与 D3 的 `cockpit-*` 一致 | ✅ |
| 一条命令可回收 | 容器可 `docker rm -f`；**但具名卷 `cockpit-mongo-data` 不随之删除**（已确认该卷存在） | ⚠️ **措辞不准** |
| 登记进 report.json 与 worklog | `report.json` `summary` 有；worklog 独立一节「自决记账：cockpit-mongo 容器」有；**均已 commit** | ✅ |

避开 27017 的理由（另两台 mongod 属他方系统、写入违反铁律 8）我复核过，成立。
**唯一要改的是措辞**：完整回收是两条命令
（`docker rm -f cockpit-mongo && docker volume rm cockpit-mongo-data`）。
「一条命令可回收」正是把这件事划进自决区的那个条件，所以这句话必须准确——
不是挑字眼，是这句话在承担判据的重量。已进清单 A-13（不阻塞）。

**记账本身合规，我不判它该叫人。**

---

## 新缺陷（本轮打回理由）

### N1 · `nexus-core` 的 arbiter report 自相矛盾，而你正要往里派活

`code/nexus-core/codeagent/arbiter/docs/report.json` 现在的内容：

```
task    = "contract v0.2（补写入侧）+ 门禁入仓"        ← 你本轮改的
summary = "契约从 v0.1 扩到 v0.2……"                  ← 你本轮改的
status  = "blocked"                                   ← 没改
escalation = { reason: "架构缺陷",
               detail: "两条独立升级，都需 CFO 裁决后本模块才能开单。…" }   ← 没改
```

`git show fff59d4 -- codeagent/arbiter/docs/report.json` 显示：你在这一笔里改了
`task`、`summary`、`reviewer_opinion.reviewer`，**唯独没碰 `status` 和 `escalation`**。

**而这一笔 commit 本身就是那两条升级的裁决：**

| 升级 | 模块 arbiter 请示的内容 | 你实际做的裁决 |
|---|---|---|
| 1 · 存储 | 「本项目没有自有 Mongo，四条出路全部越界，倾向起专属容器（绑 127.0.0.1 独立端口）」 | **你起了 `cockpit-mongo`，绑 127.0.0.1:27117** ← 就是选项本身 |
| 2 · 范围 | 「切片 1 范围与契约互斥，A：补 planner 只读面 / B：金链路终点降级，倾向 A」 | **contract v0.2 里补了 planner 只读面** ← 就是 A |

**两条都裁了，两条都执行了，但两条都没写回。** 后果是具体的：

你的下一步是「给 nexus-core 1 号 programmer 发签派活」。那个 programmer 冷启动，
会读它模块的 canonical arbiter report，读到的是
**「status: blocked，两条升级都需 CFO 裁决后本模块才能开单」**。
它只有两条路：停下来等一个早就下过的裁决，或者把已经决定的事再升级一遍。
**这与我上轮抓的 §4.1 假警报是同一个形状**——留痕指着一个已经不成立的状态，
而下一个 agent 没有理由不信它。你上轮自己写的那句话可以原样搬过来：
「假警报的危害方向是反的。」

顺带三处同一文件的小问题（一并修掉即可，不单列打回）：

- `reviewer_opinion.path` = `module_docs/worklog/2026-07-30-arbiter-gates-into-repo.md`——
  指向 arbiter **自己的 worklog**，不是 reviewer 的详报。按 report-schema 第 35 行
  该字段是「详报，仓内相对路径」，现在这一格等于没有 reviewer 落点。
- `reviewer_opinion.reviewer` 由 `module_reviewer` 改成了 `programmer_reviewer`——
  report-schema 第 33 行明写「**arbiter 的报告写 `module_reviewer`**」。改反了。
- `cross_module_impact: []`，但 contract v0.2 新增 `nexus-core.events.ingest.v1` /
  `nexus-core.timer.v1` 两个对外能力。你在**根**报告里列了四个模块，模块报告里却是空的。

**（`contract.touched: true` + `which` 四项 + `consumes: ["yq-event/v1"]` 这一格是对的，
升级面在模块层如实申报了，我不打这一格。）**

**另：该 escalation 结尾的「附带提醒」已可销账**——它当时提醒
「`contracts/yq-event-v1.md` 处于 staged 未 commit，下一轮开单前必须复核」。
我核了：该文件已被 `4c43747` 提交、当前 tracked 且工作区干净。**这条提醒可以标已核实关闭。**

**——但那条提醒的教训，本轮换了个位置又犯了一次：**

```
$ git -C code/nexus-core status --porcelain -- module_docs/
 M module_docs/worklog/2026-07-30-任务单-切片1-金链路.md
```

**你要派的那张任务单本身没有提交。** 它现在只活在 nexus-core 的工作区里：
`# 任务单 · 切片 1「金链路」…执行：codeagent/programmer/1（写区 code/backend/）·tier: hard`。

按铁律 12/18，未 commit = 未产出。一个冷启动的 programmer 拿不到 working tree 里的东西，
而这张单是它全部的任务定义。**这和上面那条「附带提醒」是同一句话——
『不能拿 staged 状态当判据』——只是这次轮到派单方自己了。**
请与 N1 一并闭环：任务单 commit 之后再发签。

### N2 · 四模块此刻全部 `gates-fresh` 红 —— 你上轮写下的教训，本轮原样复发

**硬证据**（在 `code/ring` 实跑着陆检查，不是推断）：

```
$ bash scripts/mission_complete.sh
│  ❌ 5  gates-fresh  [通用]
│       ├ 现状：模块的门禁副本已过期（框架已改，这里还是旧的）：
│       └ 怎么办：scripts/checks/_common/13-gates-fresh.sh
│                 scripts/checks/_common/90-dependency-drift.sh
└─ 🔴 着陆检查未通过 —— 拒绝提交
rc=1
```

`nginx-docker` 同样 rc=1，同样两个文件。**成因是时序，我用 sha256 定位到了：**

```
13-gates-fresh.sh   框架 HEAD f49bbb23f2f6 │ 00be505 旧版 90123ab2a279 │ ring 装的 90123ab2a279
90-dependency-drift 框架 HEAD da81564556d8 │ 00be505 旧版 70bd0a70b49d │ ring 装的 70bd0a70b49d
```

模块里装的**就是旧 v5 那一版**。reflog 给出时间线：

```
00:40  模块门禁入仓（cd4d1c0 / 061c745 / fff59d4）——此时框架还停在旧 v5
01:09  rebase (start): checkout origin/v5   ← 新 v5 (00a6d4c) 改了 13 与 90
01:16  e2e2045                              ← 之后没有任何一次重装
```

**这正是你上一轮亲手写进 worklog 的那条**：

> 「一个坑：第一次装在 rebase 之前跑，拷的是旧判据，checks/13 报『门禁在跑、在绿，
>   但跑的是旧判据』。**必须先 rebase 再装。**」

教训写下来了，一轮之后原样复发。**这不是我要挖苦——它说明这条教训现在只活在一份
worklog 的散文里，没有任何机械保证。** 真正的修法不是下次记得，而是让
`install-gates.sh`（或 arbiter-push 前置）自己拒绝在「框架仓落后于 origin」时安装，
或装完打一个指纹。这条我记进清单，属框架区（consulter）。

**为什么必须在派活前修**：你要派的 programmer，第一次 commit 就会撞上这面红墙。
它的选择是：卡住问你，或者用 `AIMERGENT_MISSION_OVERRIDE` 放行。
**后者会把逃生口用在一个纯环境问题上**——而逃生口被日常化，正是 `supervision.md`
判据表里点名的「逃生口被滥用趋势」。
补救只要一条命令（四模块各重跑 `scripts/install-gates.sh`），代价极小。

### N3 · `nginx-docker` 的 A-01 没修，但口径称四模块已完成

```
$ git -C code/nginx-docker status --porcelain -- scripts/
 M scripts/arbiter-push.sh
 M scripts/checks/_common/12-standing-docs.sh
 M scripts/checks/arbiter/80-taskcard-complete.sh
 M scripts/gates/doc-path-exempt.txt
 M scripts/hooks/pre-commit          ← 提交闸门本体
 M scripts/mission_start.sh
```

比 ring/table 上轮的情况轻——该仓 HEAD 上确实**有**三件套（早先提交的），
所以 clone 出来不是裸奔，只是**旧**。但这六个文件里包含 `pre-commit` 本体
和两条 checks，**它们的现行版本仍然只在工作区**，是我上轮报的同一个形状。

问题在口径：你的 report `summary` 写「四模块门禁 17 文件已真正入仓并复验红测」，
交接给我的说法是「（`cd4d1c0` / `061c745` / `fff59d4` / **早先 nginx-docker**）」。
「早先」这个词把一个**未完成项**说成了已完成项。**实际是 3 / 4。**

### N4 · 你改了审计方的报告，且没在 `docs_reviewed` 声明

`agents/cfo/reviewer/docs/2026-07-30-round1.md` 是我的留痕。你在 `e2e2045` 里
把其中 15 处 `` `路径:行号` `` 改写成 `` `路径` 第 N 行 ``。

**先说清楚我核到的事实，避免这条被读重：**

- 我逐段比对了committed 版本：**verdict `rejected` 在、R1/R2/R3 三条在、
  「审五件」五节在、第五件那一整段实体举证在、红线自查在，359 行结构完整，
  没有一处结论被软化或删除。** 改动确实只是那个反引号写法。
- 你在 commit message 里写了「本轮 15 处改为『`路径` 第 N 行』」，也在唤醒我时
  主动摊开来问。**这是披露过的，不是偷改。**

**但仍要打，理由是落点而不是动机：**

`agents/cfo/reviewer/docs/2026-07-30-round1.md` 出现在你 report 的 `changed_files` 里，
却**在 `docs_reviewed` 五条里一条都没有**。`docs_reviewed` 是铁律 11 的机械落点，
是「我动了哪些文档、为什么」的唯一结构化落点。**「被审方修改了审计方的报告」
恰恰是最该在那里留一行的事**——它涉及证据链，不是排版琐事。
现在这件事只存在于 commit message 的散文里和你对我的口头说明里；
六个月后有人复查这轮审计，`docs_reviewed` 会告诉他你没碰过我的报告。

正确做法（任选，都比现在好）：① `docs_reviewed` 加一条
`{path: …round1.md, action: "updated", reason: "仅路径写法适配 checks/60，未改任何结论；
原文见 <sha>"}`；② 把文件退回给我、由我改；③ 登记豁免。**你选了成本最低的一条，
但省掉了记账。**

---

## 你请我特别看的两处

### ① 「分两类」的处置 —— **对，且不是你的偏好，是成文规矩**

见上文 R2。你要找的依据在 `scripts/checks/_common/60-doc-paths-exist.sh` 第 11–13 行
和 `agents/AGENTS.md` 的文档体系表里。**你独立推导出了框架已经写死的模型，
这件事本身是好信号**——说明那个模型是自洽的，不同角色从原理出发会走到同一个结论。

### ② 「路径:行号」改写算不算变形绕过 —— **不算绕过；但它暴露了一个真缺口，你只处理了症状**

**先答问题：不算。** 用红线自己的判例来量：

> 「需要变形文案、**去引号**、隐瞒信息才能通过的『绕行』不算绕行……
>  （判例：2026-07-29 前向引用被迫去反引号——**那次规避掩盖了一个真缺口**）」

判定的核心不是「有没有改文案」，是**改完之后有没有信息被藏起来、有没有缺口被掩盖**。
逐项比：

| | 2026-07-29 那次（判为绕过） | 本轮这次 |
|---|---|---|
| 路径还在反引号里吗 | ❌ 被去掉了 | ✅ 还在 |
| 路径还受这道门管吗 | ❌ 脱离检查 | ✅ 仍被解析、仍会红 |
| 行号信息还在吗 | — | ✅ 「第 N 行」照写 |
| 掩盖了什么缺口吗 | ✅ 掩盖了一个真缺口 | ❌ 没有 |

**你的判断我复核后同意：不是绕过。** 你说「那本来就不是路径」，我把这句说得更准一点——
`file.md:33` 不是**文件路径**，是**位置引用**；检查只认识路径，认不出位置引用。
这是检查的识别能力问题，不是你写错了。

**但你只处理了症状，而这里有三层，第二三层必须记账：**

1. **检查的缺陷**：`60-doc-paths-exist.sh` 的提取正则是
   `` `(agents|code|logs|scripts)/[^`]+` ``，整串拿去 `resolves_here_or_framework`。
   解析前剥掉结尾的 `:<数字>` 就修好了——**一行**。属框架区（consulter）。
2. **它与我的角色卡直接打架**：`agents/cfo/reviewer/agent.md` 第 42 行明写
   verdict 要「**逐条 文件:行** + 违反哪条 + 具体失效场景」。
   **框架一边命令我用这个格式，一边用门禁拒绝这个格式。**
   不修的话每一轮都会重演，而每一轮的最省事解法都是「把反引号去掉」——
   **那才是红线明令禁止的那一步。今晚的压力方向是指着红线的。**
3. **它的第一个后果已经发生了**：检查的历史叙事豁免只覆盖
   `*/docs/worklog/*`、`*/docs/findings/*`、`*/docs/decisions/*`。
   consulter 的 `findings/` 在里面，**cfo_reviewer 的报告落在
   `agents/cfo/reviewer/docs/*.md`，不在里面**——于是这道门卡住了审计报告，
   于是被审方去改了审计方的报告（N4）。**根因不是你手欠，是豁免名单漏了一类角色。**

**所以：不打回你的判断，打回的是没把 2/3 记账。** 三条都已进清单（A-12），
按判据是「摩擦性重复劳动」+「有合法绕行」，我判**高危不阻塞**——
不阻塞是因为今晚能过去，高危是因为它稳定地把人往红线那一步推。

---

## 停线判定 —— **不置旗**

逐条比对 `supervision.md` 阻塞判据表：

| 判据 | 本轮 | 结论 |
|---|---|---|
| ① 门禁把合法提交/推送拦死，**且无语义正确的豁免或记账逃生口** | `gates-fresh` 确实拦死四模块提交（实跑 rc=1），**但 `AIMERGENT_MISSION_OVERRIDE` 记账逃生口存在，且真正的补救是一条 `install-gates.sh`** | 不满足 |
| ② 数据丢失路径 | 无。门禁已入仓三个、第四个 HEAD 上有旧版可用；契约已 commit | 不满足 |
| ③ 恒红（对本仓永远失败） | 否——一条命令即绿 | 不满足 |
| ④ 流程死锁 | 否 | 不满足 |
| ⑤ 密钥/安全泄露 | 否。Mongo 经 `ss` 复核仅回环监听 | 不满足 |

**未置 `logs/STOPLINE`。** 四条打回项都是十几分钟的机械修复，
不需要冻结全仓——但 **N1 与 N2 是「派活前置条件」**，请在发签之前闭环，
否则代价会转嫁给一个冷启动的 programmer。

---

## 关闭条件（复审我只看这几件）

| # | 关闭条件 |
|---|---|
| N1 | `code/nexus-core/codeagent/arbiter/docs/report.json` 的 `status` 与 `escalation` 随裁决更新（两条升级写明「CFO 已裁决：起 cockpit-mongo / 采 A 案补 planner 只读面」）；**并把任务单 `module_docs/worklog/2026-07-30-任务单-切片1-金链路.md` commit**（现为未提交）；顺带修 `reviewer_opinion.reviewer` → `module_reviewer`、`path` 指向真正的详报、`cross_module_impact` 补齐；escalation 结尾那条「契约 staged」提醒可标已核实关闭 |
| N2 | 四模块各重跑 `scripts/install-gates.sh`，并在**每个**模块实跑 `13-gates-fresh.sh` 给出 rc=0（不走管道取值） |
| N3 | `code/nginx-docker` 六个门禁文件 commit；口径改为如实描述 |
| N4 | `docs_reviewed` 补一条声明你改过 `…/2026-07-30-round1.md` 及范围 |

N2 修完 N3 会一并变绿（同一条命令），实际是三处改动。

---

## 超出本卡职权

- `60-doc-paths-exist.sh` 剥离 `:<行号>`、以及把 reviewer 报告目录纳入历史叙事豁免——
  框架区，按 J7 由人类审、consulter 改。我只举证。
- `install-gates.sh` 是否该在框架落后于 origin 时拒绝安装——框架设计，交 consulter。
- Mongo 决策本身对不对——按你的要求我只核记账，不核决定。

## 本轮红线自查

只举证不修复：除本报告、`架构问题清单.md` 增量、`agents/cfo/docs/report.json`
的 `reviewer_opinion` 一格外，未改动任何代码或文档；未置停线旗；
在 `code/ring` 与 `code/nginx-docker` 跑的检查均为只读执行，未产生任何提交或文件变更。
